# 风险登记

## R-001：设计目标与实现状态混淆（中）

- 证据和影响范围：`docs/Pinora-开发设计文档.md` 描述完整桌面工具；代码已有运行时/fake 捕获/单实例，但仍无 GPUI、真截屏、OCR。
- 触发条件：把 GPUI/Liora、OCR、Wayland Portal 等选型文字当作已实现契约。
- 失败模式：引入错误版本或未授权的外部服务，导致构建、平台兼容或权限流程不可验证。
- 缓解措施和验证：每项生产变更先建立独立计划/任务，引用源码和命令证据；依赖引入后执行 `cargo metadata`、`cargo check` 和定向测试。
- 回滚或隔离：在任务边界内撤销新增依赖和模块；保留设计文档不变。
- 负责人和状态：未分配；开放。

## R-010：历史索引与文件生命周期尚未闭环（中）

- 证据和影响范围：2026-08-02 新增 `HistoryIndex`/`HistoryStore`，支持摘要去重、条数/字节配额和 tombstone 原子索引；042 增加 H 历史窗口、单条删除、受管 PNG 完整性校验、预览和重新贴图，043 增加元数据搜索与确认清空，桌面壳仅在受监督 `SavePng` 接受完成后写入索引，并对直属受管 PNG 执行可重试 tombstone 清理。
- 触发条件：将索引存储基础误当作完整历史功能，或在文件删除失败前直接 compact。
- 失败模式：索引条目与磁盘文件分叉，或用户看见无法复用的“成功”历史条目。
- 缓解措施和验证：调用方必须先处理 `HistoryInsert.evicted`/删除返回的文件引用，再调用 `compact`；037/039/042/043 的候选、活动同名保护、PNG 摘要/尺寸校验、单条/全量删除失败和保存失败回滚测试及 workspace 门禁已通过，后续仍需补充标签、再次编辑和 GUI/桌面探针。
- 回滚或隔离动作：保留索引文件但关闭历史入口；按任务回滚 `history_store`，不影响截图、贴图和导出主路径。
- 负责人和状态：未分配；开放。

## R-002：业务测试覆盖偏窄（中）

- 证据和影响范围：2026-08-02 的 `PINORA_NO_SYSTEM_CLIPBOARD=1 cargo test --workspace` 通过 237 个可执行单元测试（app 168、core 69），覆盖运行时、贴图、fake 捕获、OS 单实例、Overlay 标注 undo/redo/直线/序号/圆角矩形、OCR 文字选择、设置面板/codec 与原子存储、历史浏览/PNG 校验/异步读取/删除事务、任务服务契约、历史文件清理和剪贴板失败语义；另有 2 项真实桌面测试忽略，仍不覆盖 GUI 与真实屏幕。
- 触发条件：开始实现 OCR 或真实平台捕获而不先锁定行为。
- 失败模式：编译通过但交互、状态转移或平台分支回归无法发现。
- 缓解措施和验证：每个功能任务先补充最小纯逻辑测试；平台集成使用隔离探针，不连接共享基础设施。
- 回滚或隔离：按任务回滚实现，保留新增回归测试以复现问题。
- 负责人和状态：未分配；开放。

## R-003：单实例仅为内存协议（中）— 已缓解（Unix）

- 证据和影响范围：主路径已用 `OsSingleInstance`（flock + Unix socket Activate）；`InMemorySingleInstance` 仅测试。
- 关闭/缓解证据：`os_instance` 测试与双进程探针确认二次启动转发 Activate（2026-07-30）。
- 残留风险：非 Unix 平台未实现；异常退出可能残留 sock（Drop/release 尽力清理）。
- 负责人和状态：未分配；Unix 已缓解，跨平台开放。

## R-004：真实捕获能力未验证（高）— fake 回退已移除

- 证据和影响范围：启动 `SelectedCaptureProvider::autodetect` 按 KDE → xcap 探测；两者均失败时进入 `Unavailable`，实际截图调用返回 `CapabilityUnavailable`，不再构造 fake 图像。
- 关闭/缓解证据：`capture_select::tests::no_real_backend_enters_unavailable_state_without_fake` 以及 workspace 质量门禁于 2026-08-01 通过。
- 残留风险：本轮未获授权运行真实桌面探针；无 pipewire/gbm、桌面权限或非 KDE 环境仍可能无法使用真实捕获，且受限 UI 恢复入口尚未完成。
- 负责人和状态：未分配；fake 回退已缓解，真实桌面验证和用户可见恢复路径开放。

## R-005：xcap 系统依赖与链接体积（低）

- 证据和影响范围：依赖 pipewire/gbm/wayland；`cargo` 依赖树显著增大。
- 触发条件：最小环境构建。
- 失败模式：缺 `.pc` 文件导致编译失败。
- 缓解措施：在 conventions 记录 dnf 安装命令；CI 安装对应 -devel。
- 负责人和状态：未分配；开放。

## R-006：桌面壳单体化导致架构不可控（高）

- 证据和影响范围：`crates/pinora-app/src/desktop_shell.rs` 当前约 3679 行，仍统一处理窗口事件、截图线程、Overlay 帧缓冲、标注、贴图、OCR、托盘和 IPC；045 已抽出 `history_window`，046 已抽出 `settings_window` 的窗口资源、草稿/预览缓存、存储调用和呈现，但 Overlay/贴图职责仍集中在 shell。
- 触发条件：继续向现有事件循环增加工具、平台分支或异步任务。
- 失败模式：输入状态互相污染、窗口关闭/重截竞态、性能修复反复回归，无法对单个能力做隔离测试。
- 缓解措施和必需验证：冻结现有 UI 行为作为回归场景；按 045 模式继续拆出设置、Overlay 和贴图窗口适配，建立应用状态机、窗口适配、Overlay 渲染、贴图控制器和异步任务边界；每层有 fake 契约测试和至少一条桌面探针。
- 回滚或隔离动作：按新 crate/模块边界分阶段迁移；旧壳保留到新主路径通过同等场景验证后再移除。
- 负责人和状态：未分配；开放。

## R-007：所谓跨平台能力实际绑定 Linux/KDE（高）

- 证据和影响范围：`src/main.rs` 和 `os_instance.rs` 直接使用 Unix socket；KDE 捕获依赖 `spectacle`、`kscreen-doctor`；剪贴板依赖 `wl-copy`/`xclip`/`xsel`；无 `cfg` 平台模块。
- 触发条件：在 Windows/macOS 或非 KDE Linux 上发布，或把 target 编译通过当作跨平台交付。
- 失败模式：无法构建、无法启动、捕获/热键/剪贴板不可用，且降级状态对用户不透明。
- 缓解措施和必需验证：建立 `PlatformServices` 能力接口和逐平台 adapter；先使 core 在所有 target 编译，再分别验证捕获、窗口、热键、剪贴板和单实例。
- 回滚或隔离动作：默认只声明已验证的平台；未验证平台隐藏为实验状态，不伪造支持。
- 负责人和状态：未分配；开放。

## R-008：发布质量门禁仍不完整（中）— 部分缓解

- 证据和影响范围：2026-08-02 已验证 `cargo fmt --check`、`cargo check --workspace` 和严格 Clippy 通过，237 个可执行单元测试通过；GUI 端到端、真实多显示器、HiDPI 和跨平台探针仍缺失，2 个真实桌面测试被忽略。
- 触发条件：继续以“静态检查和单元测试通过”作为生产完成标准。
- 失败模式：平台运行时、窗口交互、权限对话和显示缩放回归进入主分支。
- 缓解措施和必需验证：保持 fmt/Clippy/单元测试为合并门禁；补充 fake 服务契约、无显示环境启动、真实桌面探针和关键操作场景后，才评估可发布基线。
- 回滚或隔离动作：新实验代码必须在 feature/实验模块中隔离；无法自动化的真实桌面路径不得宣称已经验证。
- 负责人和状态：未分配；静态门禁已缓解，发布验证开放。

## R-009：后台任务与 UI 生命周期仍未完全收敛（中）

- 证据和影响范围：2026-08-01 OCR 适配器已从外部 `kill -9` 和无归属等待线程迁为自身 `Child` 的取消、超时、输出上限和 `wait` 回收；`OcrJobService` 与 `ExportJobService` 均已接入 `desktop_shell`，覆盖贴图/Overlay OCR、PNG 保存、图像与 OCR 文本剪贴板；剪贴板 child 使用拥有式临时文件和有界 stderr。服务保存 worker 句柄，退出先取消并在 2 秒内 join 已收敛 worker，剩余数量会显式输出。
- 触发条件：用户关闭贴图/重截/退出时后台剪贴板、导出或 OCR worker 仍在运行，或文件系统在保存中断时失败。
- 失败模式：Overlay 的 OCR、复制和保存结果现已同时受 owner、选区身份和标注 revision generation 门禁保护；同步图像复制在系统失败时返回 `ClipboardFailed` 且保留内存缓存，直接剪贴板 child 已由适配器回收，PNG 在 Linux 本地文件系统已原子发布；但忽略取消的 worker 会在有界等待后残留，复杂 OCR/剪贴板后端的子孙进程、退出卡顿和跨平台回收策略仍缺乏真实验证。
- 缓解措施和必需验证：保持剪贴板与导出经受监督任务提交；适配器在协作式取消宽限期后回收自身进程；退出测试必须证明无残留 worker/子进程或如实报告剩余数量；跨平台或断电语义需独立验证，不得从 Linux 文件测试外推。
- 回滚或隔离动作：先保留当前 OCR 服务作为隔离边界；剪贴板/导出迁移失败时关闭对应异步入口，保留同步 fake 接口，完成生命周期契约后重新开放。
- 负责人和状态：未分配；开放。

## R-011：跨平台流水线与真实桌面能力分离（高）

- 证据和影响范围：2026-08-02 已加入 Linux/macOS/Windows 原生 runner 的 CI、package、runtime-verify；`v0.1.0-preview.6` 的 CI run `30720088334`、package run `30720098823`、发布 job `91422613768`、runtime-verify run `30720250257` 均成功，10 个可执行 Release 资产及合并 SHA256 清单已逐项复核。runtime smoke 只运行 `--version` 和安装器，不创建真实 GUI 会话。
- 触发条件：把 workflow 绿色、包可安装或进程能启动当作屏幕捕获、系统剪贴板、热键、透明置顶、多显示器和权限流程已经生产可用。
- 失败模式：用户在不同桌面/权限环境中看到 capability unavailable、热键不触发、剪贴板不可用或窗口管理器行为不同。
- 缓解措施：release notes 和 runtime report 明确列出 smoke 范围；workflow 逐平台校验来源清单并生成合并 SHA256 清单；未通过真实桌面探针的平台能力不宣称等同 Linux/KDE；保留 tar/zip/raw binary 回滚分发物。
- 回滚或隔离动作：删除 038 workflow/packaging 与非 Unix IPC adapter；保留 Unix 主路径和现有实验能力。
- 负责人和状态：Neo；跨平台静态、打包、安装/卸载和版本探针已验证，真实 GUI 验证仍开放。

## R-012：设置面板仍是自绘实验适配器（中）

- 证据和影响范围：041 新增独立 softbuffer 设置窗口，支持四个字段的键盘/鼠标编辑、原子保存、失败回滚和运行时策略应用；窗口绘制使用 ASCII 点阵，不依赖原生控件，也未接入系统主题、读屏语义或平台输入法。
- 触发条件：在高 DPI、多语言输入、无障碍或非 Linux 窗口后端中把该面板当作完整生产 UI。
- 失败模式：文字可读性、焦点反馈、缩放和系统主题不符合平台约定；窗口事件在真实桌面环境下可能与 Overlay 或贴图抢焦点。
- 缓解措施和必需验证：保留纯逻辑状态机与命中测试，后续引入 UI Adapter 时复用其命令契约；在 Linux/Windows/macOS 原生会话分别做焦点、缩放和键盘探针，未验证前只标记为实验设置入口。
- 回滚或隔离动作：关闭 `S`/`Ctrl+,` 设置入口即可隔离该面板；`settings.bin` codec 和启动读取不受影响。
- 负责人和状态：未分配；静态和离线契约已验证，真实 GUI/可访问性开放。

## R-013：历史窗口渲染与真实桌面性能仍待验证（中）

- 证据和影响范围：042/049 的 H 历史窗口、受管 PNG 预览/重新贴图/再次编辑已在 051 改为 `HistoryLoadJobService` 单 worker 后台读取、摘要校验与解码；052 继续在同一 worker 按意图生成预览 XRGB、贴图 XRGB 或编辑 base/dimmed，预览不向 UI 回传 RGBA 原图。结果受条目 generation、当前选择和意图门禁，选择切换、关闭、删除、清空、配额清理、截图切换和退出均取消未交付请求。
- 触发条件：历史目录存在接近上限的大图、慢速磁盘或大量条目，或在高 DPI/非 Linux 窗口后端运行。
- 失败模式：慢读和像素准备不会阻塞事件循环，但最新请求可能等待不可抢占的旧 worker 收敛；大图仍会在单 worker 占用 CPU/内存，窗口绘制、自绘点阵文字和焦点反馈仍可能不符合平台缩放或无障碍规范。
- 缓解措施和必需验证：读取前继续执行受管路径、普通文件、长度、SHA-256、PNG RGBA8 和源矩形校验；服务契约锁定按意图输出、取消、陈旧拒绝和单 worker 限流；后续补慢盘、原生桌面、HiDPI 和读屏探针，必要时再评估缩略图与 Overlay 预处理。
- 回滚或隔离动作：关闭 H 历史入口不影响自动导出、索引写入和 tombstone 清理；回滚 `history_load_job` 只恢复同步加载，不改变索引或受管 PNG。
- 负责人和状态：Neo；离线完整性、按意图像素准备、并发语义、workspace 门禁与 GitHub 三平台 CI 已验证，渲染性能、GUI 与可访问性开放。

## R-014：托盘菜单仍缺真实跨平台证据（中）

- 证据和影响范围：044 扩展 `tray-icon` 菜单，新增设置、历史、显示/隐藏/关闭全部贴图动作；动作由 `desktop_shell` 主循环执行。050 使托盘创建失败返回 `CapabilityUnavailable`，不留下无可见入口的后台进程。
- 触发条件：Windows/macOS 或不同 Linux StatusNotifier/AppIndicator 桌面环境中的菜单事件、主题或无障碍行为与 Linux 本地实验不同。
- 失败模式：菜单项不可见、点击事件不回传或关闭/隐藏动作反馈不符合平台窗口管理约定。
- 缓解措施和验证：保留全局热键与 IPC 路径；CI 仅验证编译和版本启动，真实菜单、主题、读屏与窗口可见性仍需授权桌面探针。
- 回滚或隔离动作：移除新增菜单项和 `TrayAction` 分支，保留截图/退出与手动设置/历史入口。
- 负责人和状态：未分配；离线契约已验证，真实跨平台托盘验证开放。

## R-015：任务栏与 Dock 策略存在合成器边界（高）

- 证据和影响范围：050 已删除空闲控制窗口；054 已将主 shell、历史、设置与兼容 Overlay/贴图会话的七个生产窗口构造路径收敛至 `window_policy` 工厂。所有辅助窗口经 Windows `skip_taskbar`、X11 Utility、macOS Accessory/`LSUIElement` 和 KDE Wayland `skipTaskbar`/`skipPager` 请求隔离；KWin 脚本只按 Pinora 标题操作，并在失败时清理临时脚本。
- 触发条件：将条件编译、离线脚本测试或 GitHub runner 绿色结果当作 Windows 任务栏、macOS Dock、KDE Wayland 或其他 Wayland 合成器的真实结果。
- 失败模式：辅助窗口仍出现在任务栏、Dock 或分页器；未注册全局热键的 Wayland 用户只能通过托盘/IPC 发起截图。
- 缓解措施和必需验证：在 Windows、macOS、X11、KDE Wayland 原生会话逐一验证启动、Overlay、贴图、历史、设置、退出和多贴图；其他 Wayland 仅记录限制，不伪造支持。
- 回滚或隔离动作：保留托盘/热键/IPC；按平台回退窗口属性或 KWin 调用，不触及截图、历史、导出或贴图数据。
- 负责人和状态：Neo；054 的工厂收敛、定向策略/KWin 测试、workspace 门禁、`ctx validate` 与 GitHub CI `30734867309` 的三平台质量任务已通过；真实桌面验证开放。

## R-016：贴图真实窗口渲染性能仍待验证（中）

- 证据和影响范围：053 为每个 `PinWin` 缓存当前窗口物理尺寸与有效不透明度匹配的基础 XRGB 帧；OCR 词框、拖选和锁定边框重绘只复制缓存并叠加装饰。resize、缩放和不透明度改变会失效缓存；来源像素数量异常会受控失败。
- 触发条件：高分辨率贴图、持续 resize、多个可见贴图、HiDPI 缩放、OCR 拖选或不同平台 softbuffer 后端。
- 失败模式：无关重绘已避免重复整图缩放/压暗，但 resize 时仍在主线程重建目标尺寸帧；多个大贴图会增加一份显示尺寸 XRGB 内存；真实合成器下的输入、present 与焦点延迟未测。
- 缓解措施和必需验证：基础帧按尺寸和有效不透明度因子精确匹配，关闭贴图自动释放；离线像素和 workspace 门禁覆盖。后续需在 Windows、macOS、X11、KDE Wayland 真实会话测量 OCR 拖选、滚轮 resize、多贴图和任务栏隔离。
- 回滚或隔离动作：回退 `PinRenderCache` 与失效路径即可恢复逐帧转换；不影响 `PinId`、图像、OCR、历史、导出或窗口策略。
- 负责人和状态：Neo；离线像素、workspace 门禁、`ctx validate` 与 GitHub CI `30734583848` 的三平台质量任务已验证；真实 GUI 性能和无障碍验证开放。

## R-017：显示器菜单与真实拓扑仍待验证（中）

- 证据和影响范围：055 在托盘启动时基于 `DisplayInfo` 增加逐显示器全屏截图项目，并以内部 `DisplayId` 解析动作；desktop shell 在捕获前重新枚举显式目标，`FrameCache` 仅在 ID、origin、source rect 尺寸和 scale 完全匹配时交付。
- 触发条件：显示器热插拔、缩放/旋转变化、后端重排 ID、多个托盘实现或目标屏幕没有预热帧。
- 失败模式：菜单可能仍显示过期项目；首次指定非默认屏幕会走冷捕获；真实高 DPI 坐标与截图像素可能和离线模型不同。
- 缓解措施和必需验证：捕获前拒绝找不到的 ID，禁止回退到另一台屏幕；缓存四类拓扑错配由离线测试拒绝。后续在 Windows、macOS、X11、KDE Wayland 原生双屏/HiDPI 会话验证菜单、截图、取消、热插拔和任务栏隔离。
- 回滚或隔离动作：移除带目标的托盘项目和 `CaptureTarget`，恢复默认最大面积显示器路径；不影响数据、贴图、历史或既有热键。
- 负责人和状态：Neo；本地定向与 workspace 门禁、`ctx validate` 和 GitHub CI `30735354166` 的三平台质量任务已通过；真实多显示器和 HiDPI 验证开放。

## R-018：延时截图的真实合成器时序与可见性无法由离线测试证明（高）

- 证据和影响范围：056 以应用内 `PinWin.visible` 状态快照记录 Pinora 请求的贴图可见性；倒计时开始暂停 `FrameCache`、隐藏快照中的贴图且不创建新窗口，到期禁用缓存并在取得真实像素后恢复。离线 tray、状态机及 workspace 门禁已通过。
- 触发条件：Windows、macOS、X11 或 Wayland 合成器对 `set_visible(false)`、tray 菜单关闭、KWin 任务栏属性或窗口映射有异步行为，或窗口管理器改变可见性而 Pinora 未收到可查询状态。
- 失败模式：真实捕获仍可能短暂包含 Pinora、恢复时机造成闪烁、辅助窗口仍出现在任务栏/Dock，或应用内快照与窗口管理器实际可见状态不同。
- 缓解措施和必需验证：在四类原生会话分别验证有/无可见贴图、已有隐藏贴图、开始/取消/到期/捕获失败/退出、Overlay/设置/历史、任务栏/Dock 和多贴图；记录屏幕录制与窗口列表，失败时以平台策略修复而不是扩大 fake 测试结论。
- 回滚或隔离动作：移除延时 tray 项与 `DelayedCapture` 分支，恢复即时区域截图；不触及捕获数据、历史、设置、贴图或 `window_policy` 约束。
- 负责人和状态：Neo；离线恢复契约已验证，真实桌面验证开放。

## R-019：窗口枚举、像素捕获和 tray-only 任务栏隔离仍依赖真实桌面后端（高）

- 证据和影响范围：057 为 xcap 增加 `Window::all`、点击时二次验证和 `capture_image` 路径；菜单候选排除 Pinora 窗口、最小化与无效几何，最多保留 20 项，任何失败以稳定错误码回到 tray。离线 fake、xcap 过滤、tray 标签、桌面壳目标与 workspace 静态门禁可验证代码契约，无法创建真实 GUI 会话。
- 触发条件：Wayland/X11/Windows/macOS 的窗口权限、合成器窗口列表、xcap 元数据、窗口移动/缩放、最小化、窗口阴影或 `capture_image` 返回尺寸在真实会话中与假设不同。
- 失败模式：没有可用窗口候选、候选点击后受控失败、像素尺寸变化被拒绝，或辅助 Overlay 虽已请求跳过任务栏/Dock 仍被平台显示；实现不得用显示器截图伪装窗口截图。
- 缓解措施和必需验证：在 Windows、macOS、X11、KDE Wayland 原生会话分别验证候选过滤、目标移动/最小化/关闭、权限拒绝、真实像素来源、Overlay/贴图/设置/历史及任务栏/Dock；保留冷捕获、二次验证和无回退策略，验证失败时按平台后端关闭窗口菜单而非伪造成功。
- 回滚或隔离动作：移除 tray 的窗口候选与 `CaptureRequest::Window` 分支，保留区域、全屏、显示器指定、延时截图和已有窗口策略。
- 负责人和状态：Neo；提交 `b60ebf0` 的本地离线与静态门禁及 GitHub CI `30736791038` 已通过，真实桌面验证开放。

## R-020：捕获失败的 tray 连续驻留缺少真实 GUI 证据（高）

- 证据和影响范围：058 将 `LoadingState` 的 worker 错误、断开、预览缓冲不一致和 Overlay 建立失败从事件循环退出链中移除；恢复会清理 loading、模式、等待和帧缓存，延时路径还恢复自身快照贴图。离线分支与 workspace 门禁只能证明状态机不再调用该退出路径。
- 触发条件：真实 winit/softbuffer 在窗口部分初始化、合成器拒绝映射、GPU/呈现错误或 tray 后端异常时，与离线错误分支交错。
- 失败模式：Pinora 可能仍因范围外的渲染/主循环错误退出，或某个合成器延迟销毁 Overlay 并暂时显示任务栏/Dock 项；本任务不应把这些现象写成已解决。
- 缓解措施和必需验证：在 Windows、macOS、X11、KDE Wayland 原生会话对每个捕获入口注入/触发后端拒绝、断开和 Overlay 建立失败，检查 tray、热键、IPC、贴图恢复、窗口列表和任务栏/Dock；范围外错误另建故障隔离任务。
- 回滚或隔离动作：恢复 `LoadingState` 的旧错误传播仅会改变失败退出行为，不影响捕获数据、持久化、窗口工厂或 tray 菜单。
- 负责人和状态：Neo；提交 `36ee681` 的本地离线与静态门禁及 GitHub CI `30737231248` 已通过，真实 GUI 验证开放。

## R-021：全虚拟桌面截图的跨屏像素与窗口映射未在原生会话验证（高）

- 证据和影响范围：059 新增 `AllDisplays`：KDE `spectacle -f` 一次取像后与 KScreen/xrandr 开始快照的物理 bounds 严格比对；xcap 返回 `CapabilityUnavailable`，不将逐屏多时刻帧拼成成功资产。tray 多屏菜单进入独立虚拟桌面 Overlay，该窗口仍经 `window_policy` 请求任务栏/Dock 隔离。
- 触发条件：显示器热插拔、异构缩放、屏幕间物理空洞、KScreen 与 Spectacle 输出不同步、KWin 或其他合成器限制跨屏无边框窗口的位置/尺寸，或平台忽略任务栏/Dock 请求。
- 失败模式：Pinora 正确拒绝尺寸不匹配的截图，但真实可用拓扑可能频繁受限；或 Overlay 跨屏位置/输入错误、窗口短暂出现在任务栏/Dock，导致用户体验和 tray-only 约束不成立。
- 缓解措施和必需验证：在真实 KDE 双屏和 HiDPI 会话验证对齐/上下/间隔排列、热插拔、截取/取消/失败、Overlay 像素映射、任务栏/Dock、tray、贴图恢复和输入延迟；再在 Windows、macOS、X11 与非 KDE Wayland 验证受限能力路径，失败时按平台隐藏入口而不伪造成功。
- 回滚或隔离动作：移除 tray 的 `AllDisplays` 项和桌面状态机分支；保留单屏、区域、窗口、延时截图和既有 `window_policy`。
- 负责人和状态：Neo；离线契约、workspace 门禁与 GitHub CI `30737751448` 已通过，真实桌面验证开放。

## R-022：贴图菜单与编辑的 tray-only 窗口行为未在真实桌面验证（高）

- 证据和影响范围：060 的右键菜单仅在贴图客户区绘制，不创建系统菜单窗口；编辑 Overlay 经 `window_policy` 创建，贴图置顶只调用既有窗口层级请求。061 又删除了无 tray 的独立 Overlay/贴图会话公开入口，并以源代码门禁收敛事件循环和直接建窗位置。离线测试可证明代码可达路径与编辑恢复契约，不能观察实际任务栏、Dock 或合成器窗口列表。
- 触发条件：Windows、macOS、X11、KDE Wayland 或其他 Wayland 合成器忽略/延迟处理任务栏隔离、窗口层级或映射请求，或菜单、编辑与高 DPI 输入在高频操作时出现阻塞。
- 失败模式：Overlay、贴图或编辑窗口仍显示为新任务栏/Dock 项；编辑时原贴图恢复不及时；菜单或重新缩放造成可感知卡顿。
- 缓解措施和必需验证：将所有生产窗口继续收敛到 `window_policy`，禁止菜单创建窗口；在四类真实会话检查启动空闲、右键菜单、编辑提交/取消、置顶切换、多贴图、退出时的 tray、窗口列表、任务栏/Dock、帧时间与 HiDPI，失败后按平台修复，不以 CI 替代探针。
- 回滚或隔离动作：关闭菜单编辑入口和置顶切换，保留既有贴图快捷键与 tray 生命周期；不修改 `PinId`、图像数据或历史格式。
- 负责人和状态：Neo；离线契约已验证，真实 GUI 验证开放。

## R-023：Overlay 取色与自绘工具栏仍缺原生交互证据（中）

- 证据和影响范围：062 只从当前选区映射到的不可变原始截图采样，并将 `#RRGGBB` 交给已有受监督剪贴板任务；工具栏按画布宽度换行，无法完整容纳时直接隐藏。纯逻辑与离线测试覆盖坐标、边界、文档 revision 和任务身份，无法测量真实呈现与平台剪贴板。
- 触发条件：高 DPI、缩放后窗口客户区映射、复杂输入法/焦点、系统剪贴板后端或窄小真实选区与离线几何模型产生差异。
- 失败模式：用户看到取色坐标偏差、工具栏隐藏但无明显替代反馈、复制失败或高频取色造成输入延迟；未提交标注不会被采样是既定边界而非异常。
- 缓解措施和必需验证：保留坐标和自然尺寸布局测试，颜色更新不依赖剪贴板成功；在 Windows、macOS、X11、KDE Wayland 原生会话验证 100%/200% 缩放、窄选区、连续取色、剪贴板读回、键盘焦点和帧时间。未通过前不宣称与 Snipaste 等同。
- 回滚或隔离动作：移除 `ColorPicker` 工具和受监督文本复制调用；保留颜色循环、所有既有标注工具与工具栏。
- 负责人和状态：Neo；离线契约已验证，真实 GUI 验证开放。

## R-024：直线与序号标注仍缺真实 Overlay 交互与性能证据（中）

- 证据和影响范围：063 将直线与序号限制在既有 Overlay 标注文档、工具栏和键盘输入内；序号单击立即提交且双击不复制，生产源码窗口/事件循环边界继续由 `window_policy` 扫描。离线测试覆盖退化线、编号上限、撤销/重做不重排、工具栏命中和源码守卫，无法观察真实客户区映射或合成器行为。
- 触发条件：高 DPI、缩放、窄选区、连续快速点击、复杂焦点/输入状态、软缓冲 present 或平台窗口管理器在原生桌面中的行为与离线模型不同。
- 失败模式：直线端点或序号点击发生坐标偏差、数字可读性不足、连续标注造成可感知延迟，或 Overlay 虽未增加建窗路径仍被平台显示为任务栏/Dock 项。
- 缓解措施和必需验证：保持图像坐标和无新窗口的源码守卫；在 Windows、macOS、X11、KDE Wayland 原生会话验证 100%/200% 缩放、窄选区、连续直线/序号、撤销/重做、焦点、帧时间、tray 与任务栏/Dock。未完成前不宣称 Snipaste 级交互或平台结果。
- 回滚或隔离动作：移除 `Line`/`Number` 模型、渲染和工具栏/快捷键入口；既有标注、tray 生命周期、窗口策略和资产流程保持不变。
- 负责人和状态：Neo；离线契约与静态门禁已验证，真实 GUI 验证开放。

## R-025：圆角矩形描边仍缺原生视觉与交互证据（中）

- 证据和影响范围：064 的圆角矩形只扩展既有 Overlay 标注状态、工具栏和 `Q` 快捷键，半径在提交时冻结，代码路径没有新增窗口、截图或后台任务；距离场渲染和安全半径钳制已由离线测试覆盖，无法观察真实像素缩放或平台输入事件。
- 触发条件：不同 DPI、缩放、软缓冲实现、颜色背景、极小选区、连续拖拽或平台窗口焦点行为与离线模型不一致。
- 失败模式：圆角边缘锯齿、粗细或半径视觉不符合预期，快速拖拽造成可感知延迟，或既有 Overlay 在平台上仍进入任务栏/Dock；本任务不能被当作填充、透明度或样式配置完整实现。
- 缓解措施和必需验证：保留冻结半径、像素边界、预览一致性和 `window_policy` 源码测试；在 Windows、macOS、X11、KDE Wayland 原生会话验证 100%/200% 缩放、窄选区、连续拖拽、撤销/重做、焦点、帧时间、tray 与任务栏/Dock。未通过前不声明 Snipaste 级视觉或平台一致性。
- 回滚或隔离动作：移除 `RoundedRect` 模型、渲染和工具栏/快捷键入口；其他标注、tray 生命周期和窗口策略保持不变。
- 负责人和状态：Neo；离线契约与静态门禁已验证，真实 GUI 验证开放。

## R-026：封闭图形透明填充仍缺真实 Overlay 性能与窗口隔离证据（中）

- 证据和影响范围：065 在既有 Overlay 标注文档和工具栏中增加矩形、圆角矩形、椭圆的固定 alpha 填充；填充与描边都在现有窗口内自绘，源码没有新窗口、事件循环、截图或后台任务入口，`window_policy` 守卫持续通过。
- 触发条件：4K/HiDPI 截图、大面积连续拖拽、不同 softbuffer 后端、Windows/macOS/X11/KDE Wayland 的合成器映射和任务栏/Dock 策略延迟。
- 失败模式：主线程填充栅格化可能产生可感知延迟，或既有 Overlay/贴图虽已请求隔离仍在平台任务栏、Dock 或分页器短暂出现；离线 alpha 像素测试不能观察这些行为。
- 缓解措施和必需验证：保留边界盒遍历、预览缓存和统一 `blend_coverage`，禁止为工具栏或填充创建新窗口；在四类原生会话验证 tray 空闲、Overlay、贴图、历史、设置、连续拖拽、取消/退出、任务栏/Dock、窗口列表和帧时间。
- 回滚或隔离动作：移除填充字段、栅格化和工具栏/快捷键入口；既有描边标注、tray 生命周期和窗口策略保持不变。
- 负责人和状态：Neo；离线核心像素、工具栏、window policy、workspace 门禁已验证，真实 GUI 性能与窗口管理器证据开放。

## R-027：辅助窗口唯一展示入口仍缺真实窗口管理器证据（高）

- 证据和影响范围：066 强制所有辅助窗口隐藏创建，并将 Overlay、贴图、历史、设置和贴图恢复的展示统一到 `window_policy::show_auxiliary_window`；该入口在可见映射后对 KWin 请求 `skipTaskbar`/`skipPager`。递归源码守卫已阻止策略模块外的新建窗口、事件循环和直接可见调用。
- 触发条件：Windows、macOS、X11、KDE Wayland 或其他 Wayland 合成器在映射时忽略、延迟处理或重新设置任务栏/Dock 属性，或隐藏创建影响 Surface 首帧、焦点和输入时序。
- 失败模式：即使所有代码路径已收敛，辅助窗口仍可能短暂或持续出现在任务栏、Dock 或分页器；也可能出现首帧空白、焦点丢失、贴图恢复闪烁或无法由静态测试观察的合成器行为。
- 缓解措施和必需验证：在四类原生桌面会话验证 tray 空闲、区域/全屏/虚拟桌面/窗口 Overlay、贴图首次/批量/编辑恢复、历史、设置、取消、退出、窗口列表、任务栏/Dock、首帧、焦点和 HiDPI；失败时仅修复对应平台策略，不扩展 fake 或 CI 结论。
- 回滚或隔离动作：恢复局部展示调用只会回退可见性入口收敛；可保持工厂前置任务栏/Dock 属性、tray 生命周期和应用数据不变。
- 负责人和状态：Neo；离线源码守卫、定向测试和 workspace 门禁已验证，真实桌面窗口管理器证据开放。

## R-028：区域模糊在真实高分辨率 Overlay 中的帧时间未验证（中）

- 证据和影响范围：067 将 `B` 和工具栏入口限定于既有 tray 监督的 Overlay；模糊以原始不可变截图为源，对选择矩形及有限纵向采样边框执行水平、垂直两次滑动盒平均。离线测试已覆盖半径冻结、预览逐字节一致、反向/边界选区和区域外不变，源码没有新窗口、事件循环、截图或 worker 路径。
- 触发条件：4K/HiDPI 截图、大范围连续拖拽、低端 CPU、不同 softbuffer 后端，或 Windows/macOS/X11/KDE Wayland 的呈现和输入调度与离线环境不同。
- 失败模式：尽管算法相对选区面积线性，主线程重绘仍可能产生可感知延迟；既有 Overlay/贴图即使遵守唯一展示入口，也可能在平台任务栏、Dock 或分页器短暂出现。
- 缓解措施和必需验证：保持半径上限、有限临时缓冲和只写选区；在四类原生桌面会话验证 tray 空闲、`B` 工具栏/快捷键、连续拖拽、撤销/重做、Overlay 取消、贴图恢复、任务栏/Dock、焦点、首帧、100%/200% 缩放及帧时间。验证失败时按对应平台渲染或窗口策略修复，不将 CI 或单元测试作为桌面证据。
- 回滚或隔离动作：移除 `Blur` 标注模型、栅格化和入口；不影响既有标注、tray 生命周期、窗口策略、截图、贴图、OCR 或导出。
- 负责人和状态：Neo；离线核心/工具栏/窗口策略回归已通过，真实 GUI 性能与窗口管理器证据开放。

## R-029：Overlay 提交层缓存的真实内存与帧时间仍待验证（中）

- 证据和影响范围：068 为每个活动 Overlay 仅缓存当前源选区裁剪和其已提交 RGBA 合成层，并按源选区与 `AnnotationRevision` 失效；连续草稿拖拽不再重烧录历史文档。纯逻辑测试已验证草稿输出与完整预览逐字节一致、revision/选区重建和无效裁剪清除旧缓存。
- 触发条件：4K/HiDPI 的大选区、大量已提交标注、频繁撤销/重做或重选、可用内存紧张、不同 softbuffer 后端和原生合成器调度。
- 失败模式：缓存减少 CPU 重烧录但增加当前 Overlay 的峰值内存；草稿仍需合成、颜色转换和 damage present，实际输入或呈现延迟可能仍可感知。错误失效实现还可能显示陈旧像素，但离线缓存键测试只能覆盖已建模路径。
- 缓解措施和必需验证：维持单 Overlay 生命周期、单份裁剪和已提交层、显式重选清理、revision/选区键与无效裁剪清理；在 Windows、macOS、X11、KDE Wayland 的 100%/200% 缩放下测量连续画笔/马赛克/Blur、撤销/重做、重选、内存峰值、首帧、焦点、tray 和任务栏/Dock。不得把单元测试、CI 或代码复杂度当作帧率和窗口管理器证据。
- 回滚或隔离动作：移除 `overlay_preview_cache` 并恢复完整预览路径；标注文档、导出、贴图、tray 和窗口策略不变。
- 负责人和状态：Neo；离线像素、缓存失效、workspace 门禁已验证，真实性能和窗口管理器证据开放。

## R-030：标注清空事务与 tray-only 窗口隔离缺少原生会话证据（中）

- 证据和影响范围：069 将多条标注整体清空建模为单个内存事务，工具栏动作仅复用现有 Overlay；空闲 Pinora 只能由系统 tray 驻留，Overlay、贴图和任何辅助层均不得出现任务栏、Dock 或分页器项。离线事务、工具栏与 `window_policy` 递归守卫可证明代码路径，但不能观察窗口管理器最终状态。
- 触发条件：高 DPI、大量标注后清空/撤销/重做、Windows/macOS/X11/KDE Wayland 的窗口映射时序，或合成器忽略/延迟辅助窗口的任务栏、Dock、分页器隔离请求。
- 失败模式：清空快照增加峰值内存或主线程重绘造成可感知延迟；Overlay、贴图或其他辅助层仍可能短暂或持续出现任务栏、Dock、分页器项，从而违反 tray-only 产品约束。
- 缓解措施和必需验证：保持清空只在非空文档创建快照、草稿先取消、展示仅经 `window_policy`；在四类原生桌面会话验证空闲、区域/全屏 Overlay、贴图首次/恢复、历史、设置、清空/撤销/重做、取消与退出，并记录任务栏/Dock/分页器、tray、焦点、首帧、内存和帧时间。
- 回滚或隔离动作：移除清空工具栏入口和事务类型即可恢复逐条撤销实现；不改变截图、贴图、OCR、导出、tray 生命周期或窗口策略。
- 负责人和状态：Neo；离线事务、工具栏、窗口策略和 workspace 门禁已通过，真实桌面与性能验证开放。

## R-031：标注选择/删除命中和 tray-only 窗口隔离缺少原生会话证据（中）

- 证据和影响范围：070 在当前 Overlay 内以图像像素坐标命中最上层标注，Delete/Backspace 通过可撤销删除事务处理，选择框只覆盖呈现帧。Pinora 空闲只能由系统 tray 常驻，Overlay、贴图和任何辅助层不得出现任务栏、Dock 或分页器项。离线单元测试和 `window_policy` 源码守卫只能覆盖代码契约。
- 触发条件：高 DPI、缩放、细描边、相邻或重叠对象、大量对象、连续选择/删除/撤销/重做，以及 Windows/macOS/X11/KDE Wayland 的映射和焦点时序。
- 失败模式：用户感知的命中容差或选中框位置与高 DPI 呈现不符，主线程重绘造成延迟，或既有辅助窗口仍短暂或持续出现在任务栏、Dock 或分页器，违反 tray-only 产品约束。
- 缓解措施和必需验证：保持命中和事务在 core 中确定性计算、选择高亮不参与导出、选择索引在文档变更时失效、建窗只经 `window_policy`；在四类原生桌面验证每个标注类型、重叠对象、Delete/Backspace、undo/redo、清空、重选、取消/退出、任务栏/Dock/分页器、tray、焦点、首帧和帧时间。
- 回滚或隔离动作：移除 `Select` 工具、选择呈现和删除事务；保留既有标注、撤销/重做、清空、导出、tray 生命周期和窗口策略。
- 负责人和状态：Neo；离线核心/App/窗口策略与 workspace 门禁已通过，真实桌面与性能验证开放。

## R-032：标注移动预览与 tray-only 窗口隔离缺少原生会话证据（中）

- 证据和影响范围：071 将选中标注的拖动预览留在当前 Overlay 内存呈现层，释放时才以原位替换事务更新文档；方向键以 1/10 像素移动选中对象。Pinora 空闲仍只由系统 tray 驻留，Overlay、贴图、历史、设置及任何辅助层均不得出现任务栏、Dock 或分页器项。离线像素、事务和 `window_policy` 源码守卫只能证明代码边界。
- 触发条件：4K/HiDPI 选区、连续高频拖拽、大量前后叠放标注、选区边缘外拖、Windows/macOS/X11/KDE Wayland 的焦点和窗口映射时序，或合成器忽略/延迟任务栏、Dock、分页器隔离请求。
- 失败模式：对象坐标或选择框在缩放后偏移，连续预览仍可出现可感知延迟或内存峰值；辅助窗口可能短暂或持续出现在任务栏、Dock 或分页器，违反 tray-only 产品约束。
- 缓解措施和必需验证：维持按 `{源选区, revision, 索引}` 的前缀缓存、不可变原始像素源、释放时单事务、无新窗口路径和唯一 `window_policy` 展示入口；在 Windows、macOS、X11、KDE Wayland 原生会话验证空闲 tray、全部辅助窗口、1x/2x DPI、各标注类型、越界拖动、方向键/Shift、undo/redo、删除/清空、取消、首帧、焦点、任务栏/Dock/分页器与帧时间。
- 回滚或隔离动作：移除 `Replace` 事务、拖拽状态与替换预览，保留选择、删除、清空、既有草稿预览、导出、tray 生命周期和窗口策略。
- 负责人和状态：Neo；离线核心/App/窗口策略与 workspace 门禁已通过，真实桌面与性能验证开放。

## R-033：选区热区调整与 tray-only 窗口隔离缺少原生会话证据（中）

- 证据和影响范围：072 在当前 Overlay 中绘制并命中选区八个调整热区，核心选择模型限制 bounds、最小尺寸和反向拖动；有草稿或已提交标注时不进入该输入路径。Pinora 空闲仍只由系统 tray 驻留，Overlay、贴图、历史、设置及任何辅助层均不得出现任务栏、Dock 或分页器项。离线几何、源码窗口守卫和测试不能观察真实桌面。
- 触发条件：4K/HiDPI、极窄选区、快速连续拖拽、工具栏邻近热区、焦点切换，以及 Windows/macOS/X11/KDE Wayland 的输入缩放、映射和窗口管理器时序。
- 失败模式：热区在真实缩放下可能难以发现或命中到相邻方向，连续拖动仍可能出现帧延迟；既有辅助窗口可能短暂或持续出现在任务栏、Dock 或分页器，违反 tray-only 产品约束。
- 缓解措施和必需验证：维持核心唯一几何规则、最近热区命中、2 像素/32ms 节流、标注/草稿保护和唯一 `window_policy` 展示入口；在 Windows、macOS、X11、KDE Wayland 原生会话验证空闲 tray、1x/2x DPI、八方向、最小选区、边界、快速拖动、标注后重选、取消/完成、焦点、帧时间和任务栏/Dock/分页器。
- 回滚或隔离动作：移除 `SelectionHandle`、热区绘制和拖动分支，恢复既有重新拖选与键盘移动；不影响截图、标注、导出、OCR、贴图、tray 生命周期或窗口策略。
- 负责人和状态：Neo；离线核心/App/窗口策略与 workspace 门禁已通过，真实桌面与性能验证开放。

## 风险一：上下文漂移

- 触发条件：代码、配置或工作流变化后，没有同步更新对应上下文文件。
- 缓解措施：根据变化的事实或生命周期状态更新 `system/`、计划或任务。
- 回滚方式：只回滚上下文变更，不改变源码行为。

## 风险二：遗留迁移丢失

- 触发条件：未做语义分类就机械移动 `prompt.md`、`.memory/` 或 `.prompt/`。
- 缓解措施：先生成迁移清单；所有长期事实和可执行任务完成核对前保留来源文件。
- 回滚方式：从未改动的来源文件恢复，只删除生成的上下文文件。

## 风险三：外部依赖副作用

- 触发条件：校验或测试连接共享数据库、队列、缓存或远程接口。
- 缓解措施：优先离线检查，只运行明确授权的隔离验证。
- 回滚方式：产生副作用前停止，并在当前任务中记录阻塞证据。
