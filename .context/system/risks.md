# 风险登记

## R-001：设计目标与实现状态混淆（中）

- 证据和影响范围：`docs/Pinora-开发设计文档.md` 描述完整桌面工具；代码已有运行时/fake 捕获/单实例，但仍无 GPUI、真截屏、OCR。
- 触发条件：把 GPUI/Liora、OCR、Wayland Portal 等选型文字当作已实现契约。
- 失败模式：引入错误版本或未授权的外部服务，导致构建、平台兼容或权限流程不可验证。
- 缓解措施和验证：每项生产变更先建立独立计划/任务，引用源码和命令证据；依赖引入后执行 `cargo metadata`、`cargo check` 和定向测试。
- 回滚或隔离：在任务边界内撤销新增依赖和模块；保留设计文档不变。
- 负责人和状态：未分配；开放。

## R-010：历史索引与文件生命周期尚未闭环（中）

- 证据和影响范围：2026-08-02 新增 `HistoryIndex`/`HistoryStore`，支持摘要去重、条数/字节配额和 tombstone 原子索引；042 增加 H 历史窗口、单条删除、受管 PNG 完整性校验、预览和重新贴图，043 增加元数据搜索与确认清空，桌面壳仅在受监督 `SavePng` 接受完成后写入索引，并对直属受管 PNG 执行可重试 tombstone 清理。
- 触发条件：将索引存储基础误当作完整历史功能，或在文件删除失败前直接 compact。
- 失败模式：索引条目与磁盘文件分叉，或用户看见无法复用的“成功”历史条目。
- 缓解措施和验证：调用方必须先处理 `HistoryInsert.evicted`/删除返回的文件引用，再调用 `compact`；037/039/042/043 的候选、活动同名保护、PNG 摘要/尺寸校验、单条/全量删除失败和保存失败回滚测试及 workspace 门禁已通过，后续仍需补充标签、再次编辑和 GUI/桌面探针。
- 回滚或隔离动作：保留索引文件但关闭历史入口；按任务回滚 `history_store`，不影响截图、贴图和导出主路径。
- 负责人和状态：未分配；开放。

## R-002：业务测试覆盖偏窄（中）

- 证据和影响范围：2026-08-02 的 `PINORA_NO_SYSTEM_CLIPBOARD=1 cargo test --workspace` 通过 237 个可执行单元测试（app 168、core 69），覆盖运行时、贴图、fake 捕获、OS 单实例、Overlay 标注 undo/redo/直线/序号/圆角矩形、OCR 文字选择、设置面板/codec 与原子存储、历史浏览/PNG 校验/异步读取/删除事务、任务服务契约、历史文件清理和剪贴板失败语义；另有 2 项真实桌面测试忽略，仍不覆盖 GUI 与真实屏幕。
- 触发条件：开始实现 OCR 或真实平台捕获而不先锁定行为。
- 失败模式：编译通过但交互、状态转移或平台分支回归无法发现。
- 缓解措施和验证：每个功能任务先补充最小纯逻辑测试；平台集成使用隔离探针，不连接共享基础设施。
- 回滚或隔离：按任务回滚实现，保留新增回归测试以复现问题。
- 负责人和状态：未分配；开放。

## R-003：单实例的非 Unix 真实进程行为尚未验证（中）— Unix 已缓解

- 证据和影响范围：Unix 主路径使用 `flock` + Unix socket；非 Unix 已使用文件锁 + loopback TCP 端口文件，`InMemorySingleInstance` 仅测试。根入口不再直接依赖 Unix 类型。
- 关闭/缓解证据：`os_instance` 测试与双进程探针确认 Unix 二次启动转发 Activate（2026-07-30）；2026-08-02 Windows target 编译通过。
- 残留风险：Windows/macOS 尚无真实双进程、端口文件权限、异常退出和恢复探针；Unix 异常退出仍可能残留 socket，非 Unix 端口文件也可能陈旧。
- 负责人和状态：Neo；Unix 已缓解，非 Unix 真实桌面验证开放。

## R-004：真实捕获能力未验证（高）— fake 回退已移除

- 证据和影响范围：启动 `SelectedCaptureProvider::autodetect` 按 KDE → xcap 探测；两者均失败时进入 `Unavailable`，实际截图调用返回 `CapabilityUnavailable`，不再构造 fake 图像。
- 关闭/缓解证据：`capture_select::tests::no_real_backend_enters_unavailable_state_without_fake` 以及 workspace 质量门禁于 2026-08-01 通过。
- 残留风险：本轮未获授权运行真实桌面探针；无 pipewire/gbm、桌面权限或非 KDE 环境仍可能无法使用真实捕获。085 已提供脱敏诊断入口与固定恢复建议，但尚无系统设置跳转、完整权限/后端粒度或真实桌面交互证据。
- 负责人和状态：未分配；fake 回退与基础用户可见反馈已缓解，真实桌面验证和完整恢复路径开放。

## R-005：xcap 系统依赖与链接体积（低）

- 证据和影响范围：依赖 pipewire/gbm/wayland；`cargo` 依赖树显著增大。
- 触发条件：最小环境构建。
- 失败模式：缺 `.pc` 文件导致编译失败。
- 缓解措施：在 conventions 记录 dnf 安装命令；CI 安装对应 -devel。
- 负责人和状态：未分配；开放。

## R-006：桌面壳单体化导致架构不可控（高）

- 证据和影响范围：任务 105 已将系统集成迁入 `crates/pinora-platform`，任务 106 已将捕获后端和 `FrameCache` 迁入 `crates/pinora-capture`，任务 107 已将通用任务监督迁入 `crates/pinora-jobs`，任务 108 已将纯本地设置/历史 codec 和文件名分配迁入 `crates/pinora-storage`，任务 109 已将贴图几何、Overlay 工具栏和预览缓存迁入 `crates/pinora-desktop`，任务 110 已将 OCR CLI/TSV/词框视觉状态迁入 `crates/pinora-ocr`，任务 111 又将窗口创建/显示/KWin 隔离迁入 `pinora-desktop`，任务 112 再将面板主题、tray 能力摘要和固定反馈迁入该 crate；`crates/pinora-app/src/desktop_shell.rs` 当前约 7480 行，仍统一处理窗口事件、截图线程、Overlay 帧缓冲、标注、贴图、OCR 任务、托盘和 IPC 编排；045 已抽出 `history_window`，046 已抽出 `settings_window` 的窗口资源、草稿/预览缓存、存储调用和呈现，但 Overlay/贴图窗口、托盘、OCR 任务和事件循环职责仍集中在 shell。
- 触发条件：继续向现有事件循环增加工具、平台分支或异步任务。
- 失败模式：输入状态互相污染、窗口关闭/重截竞态、性能修复反复回归，无法对单个能力做隔离测试。
- 缓解措施和必需验证：冻结现有 UI 行为作为回归场景；按 045 模式继续拆出设置、Overlay 和贴图窗口适配，建立应用状态机、窗口适配、Overlay 渲染、贴图控制器和异步任务边界；每层有 fake 契约测试和至少一条桌面探针。
- 回滚或隔离动作：按新 crate/模块边界分阶段迁移；旧壳保留到新主路径通过同等场景验证后再移除。
- 负责人和状态：未分配；开放。

## R-007：跨平台能力仍主要绑定 Linux/KDE（高）

- 证据和影响范围：`OsSingleInstance` 已按 Unix/非 Unix 条件编译，081 又使 `GlobalHotkeyHub` 在 Windows/macOS/Linux X11 使用同一 GUI 线程生命周期；但 KDE 捕获仍依赖 `spectacle`、`kscreen-doctor`，系统剪贴板仍依赖 `wl-copy`/`xclip`/`xsel`，且没有独立 Windows/macOS/Linux `PlatformServices` adapter。
- 触发条件：在 Windows/macOS 或非 KDE Linux 上发布，或把 target 编译通过当作跨平台交付。
- 失败模式：进程可构建却仍在截图、剪贴板、热键、权限或窗口隔离上不可用；纯 Wayland 可能没有全局热键，且能力摘要对真实状态不完整。
- 缓解措施和必需验证：继续建立 `PlatformServices` 能力接口和逐平台 adapter；每项能力分别进行 target 编译、真实桌面探针和受限状态反馈，不把 GitHub runner 的静态门禁当作 GUI 验收。
- 回滚或隔离动作：默认只声明已验证的平台；未验证平台隐藏为实验状态，不伪造支持。
- 负责人和状态：Neo；跨 target 编译和热键生命周期部分缓解，逐平台真实能力开放。

## R-008：发布质量门禁仍不完整（中）— 部分缓解

- 证据和影响范围：2026-08-02 已验证 `cargo fmt --check`、`cargo check --workspace` 和严格 Clippy 通过，237 个可执行单元测试通过；GUI 端到端、真实多显示器、HiDPI 和跨平台探针仍缺失，2 个真实桌面测试被忽略。
- 触发条件：继续以“静态检查和单元测试通过”作为生产完成标准。
- 失败模式：平台运行时、窗口交互、权限对话和显示缩放回归进入主分支。
- 缓解措施和必需验证：保持 fmt/Clippy/单元测试为合并门禁；补充 fake 服务契约、无显示环境启动、真实桌面探针和关键操作场景后，才评估可发布基线。
- 回滚或隔离动作：新实验代码必须在 feature/实验模块中隔离；无法自动化的真实桌面路径不得宣称已经验证。
- 负责人和状态：未分配；静态门禁已缓解，发布验证开放。

## R-009：后台任务与 UI 生命周期仍未完全收敛（中）

- 证据和影响范围：2026-08-01 OCR 适配器已从外部 `kill -9` 和无归属等待线程迁为自身 `Child` 的取消、超时、输出上限和 `wait` 回收；`OcrJobService` 与 `ExportJobService` 均已接入 `desktop_shell`，覆盖贴图/Overlay OCR、PNG 保存、图像与 OCR 文本剪贴板；剪贴板 child 使用拥有式临时文件和有界 stderr。服务保存 worker 句柄，退出先取消并在 2 秒内 join 已收敛 worker，剩余数量会显式输出。
- 触发条件：用户关闭贴图/重截/退出时后台剪贴板、导出或 OCR worker 仍在运行，或文件系统在保存中断时失败。
- 失败模式：Overlay 的 OCR、复制和保存结果现已同时受 owner、选区身份和标注 revision generation 门禁保护；同步图像复制在系统失败时返回 `ClipboardFailed` 且保留内存缓存，直接剪贴板 child 已由适配器回收，PNG 在 Linux 本地文件系统已原子发布；但忽略取消的 worker 会在有界等待后残留，复杂 OCR/剪贴板后端的子孙进程、退出卡顿和跨平台回收策略仍缺乏真实验证。
- 缓解措施和必需验证：保持剪贴板与导出经受监督任务提交；适配器在协作式取消宽限期后回收自身进程；退出测试必须证明无残留 worker/子进程或如实报告剩余数量；跨平台或断电语义需独立验证，不得从 Linux 文件测试外推。
- 回滚或隔离动作：先保留当前 OCR 服务作为隔离边界；剪贴板/导出迁移失败时关闭对应异步入口，保留同步 fake 接口，完成生命周期契约后重新开放。
- 负责人和状态：未分配；开放。

## R-011：跨平台流水线与真实桌面能力分离（高）

- 证据和影响范围：2026-08-02 已加入 Linux/macOS/Windows 原生 runner 的 CI、package、runtime-verify；`v0.1.0-preview.6` 的 CI run `30720088334`、package run `30720098823`、发布 job `91422613768`、runtime-verify run `30720250257` 均成功，10 个可执行 Release 资产及合并 SHA256 清单已逐项复核。runtime smoke 只运行 `--version` 和安装器，不创建真实 GUI 会话。
- 触发条件：把 workflow 绿色、包可安装或进程能启动当作屏幕捕获、系统剪贴板、热键、透明置顶、多显示器和权限流程已经生产可用。
- 失败模式：用户在不同桌面/权限环境中看到 capability unavailable、热键不触发、剪贴板不可用或窗口管理器行为不同。
- 缓解措施：release notes 和 runtime report 明确列出 smoke 范围；workflow 逐平台校验来源清单并生成合并 SHA256 清单；未通过真实桌面探针的平台能力不宣称等同 Linux/KDE；保留 tar/zip/raw binary 回滚分发物。
- 回滚或隔离动作：删除 038 workflow/packaging 与非 Unix IPC adapter；保留 Unix 主路径和现有实验能力。
- 负责人和状态：Neo；跨平台静态、打包、安装/卸载和版本探针已验证，真实 GUI 验证仍开放。

## R-012：辅助面板主题与无障碍仍缺原生桌面证据（中）

- 证据和影响范围：041 新增独立 softbuffer 设置窗口；087 让设置、历史和诊断共享自绘 `PanelTheme`，设置草稿即时预览，历史/诊断只在原子保存成功后刷新。三个窗口创建时读取 `Window::theme()`，仅在 `ThemeMode::System` 下响应 `ThemeChanged`，未知外观回退 Dark。绘制仍使用 ASCII 点阵，不具备原生控件语义。
- 触发条件：高 DPI、缩放、原生标题栏、系统深浅色切换、色彩管理、多语言输入、读屏或不同 Linux/Windows/macOS 窗口后端与 winit/softbuffer 行为不一致。
- 失败模式：实际主题事件可能不投递或延迟，文字对比度、焦点反馈、缩放与无障碍语义可能不符合平台约定；辅助窗口也可能在真实桌面环境下与 Overlay 或贴图发生焦点、任务栏/Dock/分页器行为差异。
- 缓解措施和必需验证：保持纯主题解析、浅深帧差异、草稿/成功保存/失败保存和窗口策略测试；在 Linux X11、KDE Wayland、Windows、macOS 原生会话验证 System/Light/Dark、运行时系统切换、保存失败、窗口重开、键盘焦点、100%/200% 缩放、读屏、任务栏/Dock/分页器与首帧。未验证前只标记为实验辅助面板主题。
- 回滚或隔离动作：移除 `panel_theme` 与窗口刷新接线，恢复固定深色；`ThemeMode`、`settings.bin` codec、历史、诊断内容、截图、贴图和窗口策略不变。
- 负责人和状态：Neo；离线主题、保存传播、窗口策略、workspace 严格门禁和 Windows target 已验证，真实 GUI/可访问性开放。

## R-013：历史窗口渲染与真实桌面性能仍待验证（中）

- 证据和影响范围：042/049 的 H 历史窗口、受管 PNG 预览/重新贴图/再次编辑已在 051 改为 `HistoryLoadJobService` 单 worker 后台读取、摘要校验与解码；052 继续在同一 worker 按意图生成预览 XRGB、贴图 XRGB 或编辑 base/dimmed，预览不向 UI 回传 RGBA 原图。结果受条目 generation、当前选择和意图门禁，选择切换、关闭、删除、清空、配额清理、截图切换和退出均取消未交付请求。
- 触发条件：历史目录存在接近上限的大图、慢速磁盘或大量条目，或在高 DPI/非 Linux 窗口后端运行。
- 失败模式：慢读和像素准备不会阻塞事件循环，但最新请求可能等待不可抢占的旧 worker 收敛；大图仍会在单 worker 占用 CPU/内存，窗口绘制、自绘点阵文字和焦点反馈仍可能不符合平台缩放或无障碍规范。
- 缓解措施和必需验证：读取前继续执行受管路径、普通文件、长度、SHA-256、PNG RGBA8 和源矩形校验；服务契约锁定按意图输出、取消、陈旧拒绝和单 worker 限流；后续补慢盘、原生桌面、HiDPI 和读屏探针，必要时再评估缩略图与 Overlay 预处理。
- 回滚或隔离动作：关闭 H 历史入口不影响自动导出、索引写入和 tombstone 清理；回滚 `history_load_job` 只恢复同步加载，不改变索引或受管 PNG。
- 负责人和状态：Neo；离线完整性、按意图像素准备、并发语义、workspace 门禁与 GitHub 三平台 CI 已验证，渲染性能、GUI 与可访问性开放。

## R-014：托盘菜单仍缺真实跨平台证据（中）

- 证据和影响范围：044 扩展 `tray-icon` 菜单，新增设置、历史、显示/隐藏/关闭全部贴图动作；动作由 `desktop_shell` 主循环执行。050 使托盘创建失败返回 `CapabilityUnavailable`，不留下无可见入口的后台进程。
- 触发条件：Windows/macOS 或不同 Linux StatusNotifier/AppIndicator 桌面环境中的菜单事件、主题或无障碍行为与 Linux 本地实验不同。
- 失败模式：菜单项不可见、点击事件不回传或关闭/隐藏动作反馈不符合平台窗口管理约定。
- 缓解措施和验证：保留全局热键与 IPC 路径；CI 仅验证编译和版本启动，真实菜单、主题、读屏与窗口可见性仍需授权桌面探针。
- 回滚或隔离动作：移除新增菜单项和 `TrayAction` 分支，保留截图/退出与手动设置/历史入口。
- 负责人和状态：未分配；离线契约已验证，真实跨平台托盘验证开放。

## R-015：任务栏与 Dock 策略存在合成器边界（高）

- 证据和影响范围：050 已删除空闲控制窗口；054 已将主 shell、历史、设置与兼容 Overlay/贴图会话的七个生产窗口构造路径收敛至 `window_policy` 工厂。所有辅助窗口经 Windows `skip_taskbar`、X11 Utility、macOS Accessory/`LSUIElement` 和 KDE Wayland `skipTaskbar`/`skipPager` 请求隔离；KWin 脚本只按 Pinora 标题操作，并在失败时清理临时脚本。
- 触发条件：将条件编译、离线脚本测试或 GitHub runner 绿色结果当作 Windows 任务栏、macOS Dock、KDE Wayland 或其他 Wayland 合成器的真实结果。
- 失败模式：辅助窗口仍出现在任务栏、Dock 或分页器；未注册全局热键的 Wayland 用户只能通过托盘/IPC 发起截图。
- 缓解措施和必需验证：在 Windows、macOS、X11、KDE Wayland 原生会话逐一验证启动、Overlay、贴图、历史、设置、退出和多贴图；其他 Wayland 仅记录限制，不伪造支持。
- 回滚或隔离动作：保留托盘/热键/IPC；按平台回退窗口属性或 KWin 调用，不触及截图、历史、导出或贴图数据。
- 负责人和状态：Neo；054 的工厂收敛、定向策略/KWin 测试、workspace 门禁、`ctx validate` 与 GitHub CI `30734867309` 的三平台质量任务已通过；真实桌面验证开放。

## R-016：贴图真实窗口渲染性能仍待验证（中）

- 证据和影响范围：053 为每个 `PinWin` 缓存当前窗口物理尺寸与有效不透明度匹配的基础 XRGB 帧；OCR 词框、拖选和锁定边框重绘只复制缓存并叠加装饰。resize、缩放和不透明度改变会失效缓存；来源像素数量异常会受控失败。
- 触发条件：高分辨率贴图、持续 resize、多个可见贴图、HiDPI 缩放、OCR 拖选或不同平台 softbuffer 后端。
- 失败模式：无关重绘已避免重复整图缩放/压暗，但 resize 时仍在主线程重建目标尺寸帧；多个大贴图会增加一份显示尺寸 XRGB 内存；真实合成器下的输入、present 与焦点延迟未测。
- 缓解措施和必需验证：基础帧按尺寸和有效不透明度因子精确匹配，关闭贴图自动释放；离线像素和 workspace 门禁覆盖。后续需在 Windows、macOS、X11、KDE Wayland 真实会话测量 OCR 拖选、滚轮 resize、多贴图和任务栏隔离。
- 回滚或隔离动作：回退 `PinRenderCache` 与失效路径即可恢复逐帧转换；不影响 `PinId`、图像、OCR、历史、导出或窗口策略。
- 负责人和状态：Neo；离线像素、workspace 门禁、`ctx validate` 与 GitHub CI `30734583848` 的三平台质量任务已验证；真实 GUI 性能和无障碍验证开放。

## R-017：显示器菜单与真实拓扑仍待验证（中）

- 证据和影响范围：055 在托盘启动时基于 `DisplayInfo` 增加逐显示器全屏截图项目，并以内部 `DisplayId` 解析动作；desktop shell 在捕获前重新枚举显式目标，`FrameCache` 仅在 ID、origin、source rect 尺寸和 scale 完全匹配时交付。
- 触发条件：显示器热插拔、缩放/旋转变化、后端重排 ID、多个托盘实现或目标屏幕没有预热帧。
- 失败模式：菜单可能仍显示过期项目；首次指定非默认屏幕会走冷捕获；真实高 DPI 坐标与截图像素可能和离线模型不同。
- 缓解措施和必需验证：捕获前拒绝找不到的 ID，禁止回退到另一台屏幕；缓存四类拓扑错配由离线测试拒绝。后续在 Windows、macOS、X11、KDE Wayland 原生双屏/HiDPI 会话验证菜单、截图、取消、热插拔和任务栏隔离。
- 回滚或隔离动作：移除带目标的托盘项目和 `CaptureTarget`，恢复默认最大面积显示器路径；不影响数据、贴图、历史或既有热键。
- 负责人和状态：Neo；本地定向与 workspace 门禁、`ctx validate` 和 GitHub CI `30735354166` 的三平台质量任务已通过；真实多显示器和 HiDPI 验证开放。

## R-018：延时截图的真实合成器时序与可见性无法由离线测试证明（高）

- 证据和影响范围：056 以应用内 `PinWin.visible` 状态快照记录 Pinora 请求的贴图可见性；倒计时开始暂停 `FrameCache`、隐藏快照中的贴图且不创建新窗口，到期禁用缓存并在取得真实像素后恢复。离线 tray、状态机及 workspace 门禁已通过。
- 触发条件：Windows、macOS、X11 或 Wayland 合成器对 `set_visible(false)`、tray 菜单关闭、KWin 任务栏属性或窗口映射有异步行为，或窗口管理器改变可见性而 Pinora 未收到可查询状态。
- 失败模式：真实捕获仍可能短暂包含 Pinora、恢复时机造成闪烁、辅助窗口仍出现在任务栏/Dock，或应用内快照与窗口管理器实际可见状态不同。
- 缓解措施和必需验证：在四类原生会话分别验证有/无可见贴图、已有隐藏贴图、开始/取消/到期/捕获失败/退出、Overlay/设置/历史、任务栏/Dock 和多贴图；记录屏幕录制与窗口列表，失败时以平台策略修复而不是扩大 fake 测试结论。
- 回滚或隔离动作：移除延时 tray 项与 `DelayedCapture` 分支，恢复即时区域截图；不触及捕获数据、历史、设置、贴图或 `window_policy` 约束。
- 负责人和状态：Neo；离线恢复契约已验证，真实桌面验证开放。

## R-019：窗口枚举、像素捕获和 tray-only 任务栏隔离仍依赖真实桌面后端（高）

- 证据和影响范围：057 为 xcap 增加 `Window::all`、点击时二次验证和 `capture_image` 路径；菜单候选排除 Pinora 窗口、最小化与无效几何，最多保留 20 项，任何失败以稳定错误码回到 tray。离线 fake、xcap 过滤、tray 标签、桌面壳目标与 workspace 静态门禁可验证代码契约，无法创建真实 GUI 会话。
- 触发条件：Wayland/X11/Windows/macOS 的窗口权限、合成器窗口列表、xcap 元数据、窗口移动/缩放、最小化、窗口阴影或 `capture_image` 返回尺寸在真实会话中与假设不同。
- 失败模式：没有可用窗口候选、候选点击后受控失败、像素尺寸变化被拒绝，或辅助 Overlay 虽已请求跳过任务栏/Dock 仍被平台显示；实现不得用显示器截图伪装窗口截图。
- 缓解措施和必需验证：在 Windows、macOS、X11、KDE Wayland 原生会话分别验证候选过滤、目标移动/最小化/关闭、权限拒绝、真实像素来源、Overlay/贴图/设置/历史及任务栏/Dock；保留冷捕获、二次验证和无回退策略，验证失败时按平台后端关闭窗口菜单而非伪造成功。
- 回滚或隔离动作：移除 tray 的窗口候选与 `CaptureRequest::Window` 分支，保留区域、全屏、显示器指定、延时截图和已有窗口策略。
- 负责人和状态：Neo；提交 `b60ebf0` 的本地离线与静态门禁及 GitHub CI `30736791038` 已通过，真实桌面验证开放。

## R-020：捕获失败的 tray 连续驻留缺少真实 GUI 证据（高）

- 证据和影响范围：058 将 `LoadingState` 的 worker 错误、断开、预览缓冲不一致和 Overlay 建立失败从事件循环退出链中移除；恢复会清理 loading、模式、等待和帧缓存，延时路径还恢复自身快照贴图。离线分支与 workspace 门禁只能证明状态机不再调用该退出路径。
- 触发条件：真实 winit/softbuffer 在窗口部分初始化、合成器拒绝映射、GPU/呈现错误或 tray 后端异常时，与离线错误分支交错。
- 失败模式：Pinora 可能仍因范围外的渲染/主循环错误退出，或某个合成器延迟销毁 Overlay 并暂时显示任务栏/Dock 项；本任务不应把这些现象写成已解决。
- 缓解措施和必需验证：在 Windows、macOS、X11、KDE Wayland 原生会话对每个捕获入口注入/触发后端拒绝、断开和 Overlay 建立失败，检查 tray、热键、IPC、贴图恢复、窗口列表和任务栏/Dock；范围外错误另建故障隔离任务。
- 回滚或隔离动作：恢复 `LoadingState` 的旧错误传播仅会改变失败退出行为，不影响捕获数据、持久化、窗口工厂或 tray 菜单。
- 负责人和状态：Neo；提交 `36ee681` 的本地离线与静态门禁及 GitHub CI `30737231248` 已通过，真实 GUI 验证开放。

## R-021：全虚拟桌面截图的跨屏像素与窗口映射未在原生会话验证（高）

- 证据和影响范围：059 新增 `AllDisplays`：KDE `spectacle -f` 一次取像后与 KScreen/xrandr 开始快照的物理 bounds 严格比对；xcap 返回 `CapabilityUnavailable`，不将逐屏多时刻帧拼成成功资产。tray 多屏菜单进入独立虚拟桌面 Overlay，该窗口仍经 `window_policy` 请求任务栏/Dock 隔离。
- 触发条件：显示器热插拔、异构缩放、屏幕间物理空洞、KScreen 与 Spectacle 输出不同步、KWin 或其他合成器限制跨屏无边框窗口的位置/尺寸，或平台忽略任务栏/Dock 请求。
- 失败模式：Pinora 正确拒绝尺寸不匹配的截图，但真实可用拓扑可能频繁受限；或 Overlay 跨屏位置/输入错误、窗口短暂出现在任务栏/Dock，导致用户体验和 tray-only 约束不成立。
- 缓解措施和必需验证：在真实 KDE 双屏和 HiDPI 会话验证对齐/上下/间隔排列、热插拔、截取/取消/失败、Overlay 像素映射、任务栏/Dock、tray、贴图恢复和输入延迟；再在 Windows、macOS、X11 与非 KDE Wayland 验证受限能力路径，失败时按平台隐藏入口而不伪造成功。
- 回滚或隔离动作：移除 tray 的 `AllDisplays` 项和桌面状态机分支；保留单屏、区域、窗口、延时截图和既有 `window_policy`。
- 负责人和状态：Neo；离线契约、workspace 门禁与 GitHub CI `30737751448` 已通过，真实桌面验证开放。

## R-022：贴图菜单与编辑的 tray-only 窗口行为未在真实桌面验证（高）

- 证据和影响范围：060 的右键菜单仅在贴图客户区绘制，不创建系统菜单窗口；编辑 Overlay 经 `window_policy` 创建，贴图置顶只调用既有窗口层级请求。061 又删除了无 tray 的独立 Overlay/贴图会话公开入口，并以源代码门禁收敛事件循环和直接建窗位置。离线测试可证明代码可达路径与编辑恢复契约，不能观察实际任务栏、Dock 或合成器窗口列表。
- 触发条件：Windows、macOS、X11、KDE Wayland 或其他 Wayland 合成器忽略/延迟处理任务栏隔离、窗口层级或映射请求，或菜单、编辑与高 DPI 输入在高频操作时出现阻塞。
- 失败模式：Overlay、贴图或编辑窗口仍显示为新任务栏/Dock 项；编辑时原贴图恢复不及时；菜单或重新缩放造成可感知卡顿。
- 缓解措施和必需验证：将所有生产窗口继续收敛到 `window_policy`，禁止菜单创建窗口；在四类真实会话检查启动空闲、右键菜单、编辑提交/取消、置顶切换、多贴图、退出时的 tray、窗口列表、任务栏/Dock、帧时间与 HiDPI，失败后按平台修复，不以 CI 替代探针。
- 回滚或隔离动作：关闭菜单编辑入口和置顶切换，保留既有贴图快捷键与 tray 生命周期；不修改 `PinId`、图像数据或历史格式。
- 负责人和状态：Neo；离线契约已验证，真实 GUI 验证开放。

## R-023：Overlay 取色与自绘工具栏仍缺原生交互证据（中）

- 证据和影响范围：062 只从当前选区映射到的不可变原始截图采样，并将 `#RRGGBB` 交给已有受监督剪贴板任务；工具栏按画布宽度换行，无法完整容纳时直接隐藏。纯逻辑与离线测试覆盖坐标、边界、文档 revision 和任务身份，无法测量真实呈现与平台剪贴板。
- 触发条件：高 DPI、缩放后窗口客户区映射、复杂输入法/焦点、系统剪贴板后端或窄小真实选区与离线几何模型产生差异。
- 失败模式：用户看到取色坐标偏差、工具栏隐藏但无明显替代反馈、复制失败或高频取色造成输入延迟；未提交标注不会被采样是既定边界而非异常。
- 缓解措施和必需验证：保留坐标和自然尺寸布局测试，颜色更新不依赖剪贴板成功；在 Windows、macOS、X11、KDE Wayland 原生会话验证 100%/200% 缩放、窄选区、连续取色、剪贴板读回、键盘焦点和帧时间。未通过前不宣称与 Snipaste 等同。
- 回滚或隔离动作：移除 `ColorPicker` 工具和受监督文本复制调用；保留颜色循环、所有既有标注工具与工具栏。
- 负责人和状态：Neo；离线契约已验证，真实 GUI 验证开放。

## R-024：直线与序号标注仍缺真实 Overlay 交互与性能证据（中）

- 证据和影响范围：063 将直线与序号限制在既有 Overlay 标注文档、工具栏和键盘输入内；序号单击立即提交且双击不复制，生产源码窗口/事件循环边界继续由 `window_policy` 扫描。离线测试覆盖退化线、编号上限、撤销/重做不重排、工具栏命中和源码守卫，无法观察真实客户区映射或合成器行为。
- 触发条件：高 DPI、缩放、窄选区、连续快速点击、复杂焦点/输入状态、软缓冲 present 或平台窗口管理器在原生桌面中的行为与离线模型不同。
- 失败模式：直线端点或序号点击发生坐标偏差、数字可读性不足、连续标注造成可感知延迟，或 Overlay 虽未增加建窗路径仍被平台显示为任务栏/Dock 项。
- 缓解措施和必需验证：保持图像坐标和无新窗口的源码守卫；在 Windows、macOS、X11、KDE Wayland 原生会话验证 100%/200% 缩放、窄选区、连续直线/序号、撤销/重做、焦点、帧时间、tray 与任务栏/Dock。未完成前不宣称 Snipaste 级交互或平台结果。
- 回滚或隔离动作：移除 `Line`/`Number` 模型、渲染和工具栏/快捷键入口；既有标注、tray 生命周期、窗口策略和资产流程保持不变。
- 负责人和状态：Neo；离线契约与静态门禁已验证，真实 GUI 验证开放。

## R-025：圆角矩形描边仍缺原生视觉与交互证据（中）

- 证据和影响范围：064 的圆角矩形只扩展既有 Overlay 标注状态、工具栏和 `Q` 快捷键，半径在提交时冻结，代码路径没有新增窗口、截图或后台任务；距离场渲染和安全半径钳制已由离线测试覆盖，无法观察真实像素缩放或平台输入事件。
- 触发条件：不同 DPI、缩放、软缓冲实现、颜色背景、极小选区、连续拖拽或平台窗口焦点行为与离线模型不一致。
- 失败模式：圆角边缘锯齿、粗细或半径视觉不符合预期，快速拖拽造成可感知延迟，或既有 Overlay 在平台上仍进入任务栏/Dock；本任务不能被当作填充、透明度或样式配置完整实现。
- 缓解措施和必需验证：保留冻结半径、像素边界、预览一致性和 `window_policy` 源码测试；在 Windows、macOS、X11、KDE Wayland 原生会话验证 100%/200% 缩放、窄选区、连续拖拽、撤销/重做、焦点、帧时间、tray 与任务栏/Dock。未通过前不声明 Snipaste 级视觉或平台一致性。
- 回滚或隔离动作：移除 `RoundedRect` 模型、渲染和工具栏/快捷键入口；其他标注、tray 生命周期和窗口策略保持不变。
- 负责人和状态：Neo；离线契约与静态门禁已验证，真实 GUI 验证开放。

## R-026：封闭图形透明填充仍缺真实 Overlay 性能与窗口隔离证据（中）

- 证据和影响范围：065 在既有 Overlay 标注文档和工具栏中增加矩形、圆角矩形、椭圆的固定 alpha 填充；填充与描边都在现有窗口内自绘，源码没有新窗口、事件循环、截图或后台任务入口，`window_policy` 守卫持续通过。
- 触发条件：4K/HiDPI 截图、大面积连续拖拽、不同 softbuffer 后端、Windows/macOS/X11/KDE Wayland 的合成器映射和任务栏/Dock 策略延迟。
- 失败模式：主线程填充栅格化可能产生可感知延迟，或既有 Overlay/贴图虽已请求隔离仍在平台任务栏、Dock 或分页器短暂出现；离线 alpha 像素测试不能观察这些行为。
- 缓解措施和必需验证：保留边界盒遍历、预览缓存和统一 `blend_coverage`，禁止为工具栏或填充创建新窗口；在四类原生会话验证 tray 空闲、Overlay、贴图、历史、设置、连续拖拽、取消/退出、任务栏/Dock、窗口列表和帧时间。
- 回滚或隔离动作：移除填充字段、栅格化和工具栏/快捷键入口；既有描边标注、tray 生命周期和窗口策略保持不变。
- 负责人和状态：Neo；离线核心像素、工具栏、window policy、workspace 门禁已验证，真实 GUI 性能与窗口管理器证据开放。

## R-027：辅助窗口唯一展示入口仍缺真实窗口管理器证据（高）

- 证据和影响范围：066 强制所有辅助窗口隐藏创建，并将 Overlay、贴图、历史、设置和贴图恢复的展示统一到 `window_policy::show_auxiliary_window`；该入口在可见映射后对 KWin 请求 `skipTaskbar`/`skipPager`。递归源码守卫已阻止策略模块外的新建窗口、事件循环和直接可见调用。
- 触发条件：Windows、macOS、X11、KDE Wayland 或其他 Wayland 合成器在映射时忽略、延迟处理或重新设置任务栏/Dock 属性，或隐藏创建影响 Surface 首帧、焦点和输入时序。
- 失败模式：即使所有代码路径已收敛，辅助窗口仍可能短暂或持续出现在任务栏、Dock 或分页器；也可能出现首帧空白、焦点丢失、贴图恢复闪烁或无法由静态测试观察的合成器行为。
- 缓解措施和必需验证：在四类原生桌面会话验证 tray 空闲、区域/全屏/虚拟桌面/窗口 Overlay、贴图首次/批量/编辑恢复、历史、设置、取消、退出、窗口列表、任务栏/Dock、首帧、焦点和 HiDPI；失败时仅修复对应平台策略，不扩展 fake 或 CI 结论。
- 回滚或隔离动作：恢复局部展示调用只会回退可见性入口收敛；可保持工厂前置任务栏/Dock 属性、tray 生命周期和应用数据不变。
- 负责人和状态：Neo；离线源码守卫、定向测试和 workspace 门禁已验证，真实桌面窗口管理器证据开放。

## R-028：区域模糊在真实高分辨率 Overlay 中的帧时间未验证（中）

- 证据和影响范围：067 将 `B` 和工具栏入口限定于既有 tray 监督的 Overlay；模糊以原始不可变截图为源，对选择矩形及有限纵向采样边框执行水平、垂直两次滑动盒平均。离线测试已覆盖半径冻结、预览逐字节一致、反向/边界选区和区域外不变，源码没有新窗口、事件循环、截图或 worker 路径。
- 触发条件：4K/HiDPI 截图、大范围连续拖拽、低端 CPU、不同 softbuffer 后端，或 Windows/macOS/X11/KDE Wayland 的呈现和输入调度与离线环境不同。
- 失败模式：尽管算法相对选区面积线性，主线程重绘仍可能产生可感知延迟；既有 Overlay/贴图即使遵守唯一展示入口，也可能在平台任务栏、Dock 或分页器短暂出现。
- 缓解措施和必需验证：保持半径上限、有限临时缓冲和只写选区；在四类原生桌面会话验证 tray 空闲、`B` 工具栏/快捷键、连续拖拽、撤销/重做、Overlay 取消、贴图恢复、任务栏/Dock、焦点、首帧、100%/200% 缩放及帧时间。验证失败时按对应平台渲染或窗口策略修复，不将 CI 或单元测试作为桌面证据。
- 回滚或隔离动作：移除 `Blur` 标注模型、栅格化和入口；不影响既有标注、tray 生命周期、窗口策略、截图、贴图、OCR 或导出。
- 负责人和状态：Neo；离线核心/工具栏/窗口策略回归已通过，真实 GUI 性能与窗口管理器证据开放。

## R-029：Overlay 提交层缓存的真实内存与帧时间仍待验证（中）

- 证据和影响范围：068 为每个活动 Overlay 仅缓存当前源选区裁剪和其已提交 RGBA 合成层，并按源选区与 `AnnotationRevision` 失效；连续草稿拖拽不再重烧录历史文档。纯逻辑测试已验证草稿输出与完整预览逐字节一致、revision/选区重建和无效裁剪清除旧缓存。
- 触发条件：4K/HiDPI 的大选区、大量已提交标注、频繁撤销/重做或重选、可用内存紧张、不同 softbuffer 后端和原生合成器调度。
- 失败模式：缓存减少 CPU 重烧录但增加当前 Overlay 的峰值内存；草稿仍需合成、颜色转换和 damage present，实际输入或呈现延迟可能仍可感知。错误失效实现还可能显示陈旧像素，但离线缓存键测试只能覆盖已建模路径。
- 缓解措施和必需验证：维持单 Overlay 生命周期、单份裁剪和已提交层、显式重选清理、revision/选区键与无效裁剪清理；在 Windows、macOS、X11、KDE Wayland 的 100%/200% 缩放下测量连续画笔/马赛克/Blur、撤销/重做、重选、内存峰值、首帧、焦点、tray 和任务栏/Dock。不得把单元测试、CI 或代码复杂度当作帧率和窗口管理器证据。
- 回滚或隔离动作：移除 `overlay_preview_cache` 并恢复完整预览路径；标注文档、导出、贴图、tray 和窗口策略不变。
- 负责人和状态：Neo；离线像素、缓存失效、workspace 门禁已验证，真实性能和窗口管理器证据开放。

## R-030：标注清空事务与 tray-only 窗口隔离缺少原生会话证据（中）

- 证据和影响范围：069 将多条标注整体清空建模为单个内存事务，工具栏动作仅复用现有 Overlay；空闲 Pinora 只能由系统 tray 驻留，Overlay、贴图和任何辅助层均不得出现任务栏、Dock 或分页器项。离线事务、工具栏与 `window_policy` 递归守卫可证明代码路径，但不能观察窗口管理器最终状态。
- 触发条件：高 DPI、大量标注后清空/撤销/重做、Windows/macOS/X11/KDE Wayland 的窗口映射时序，或合成器忽略/延迟辅助窗口的任务栏、Dock、分页器隔离请求。
- 失败模式：清空快照增加峰值内存或主线程重绘造成可感知延迟；Overlay、贴图或其他辅助层仍可能短暂或持续出现任务栏、Dock、分页器项，从而违反 tray-only 产品约束。
- 缓解措施和必需验证：保持清空只在非空文档创建快照、草稿先取消、展示仅经 `window_policy`；在四类原生桌面会话验证空闲、区域/全屏 Overlay、贴图首次/恢复、历史、设置、清空/撤销/重做、取消与退出，并记录任务栏/Dock/分页器、tray、焦点、首帧、内存和帧时间。
- 回滚或隔离动作：移除清空工具栏入口和事务类型即可恢复逐条撤销实现；不改变截图、贴图、OCR、导出、tray 生命周期或窗口策略。
- 负责人和状态：Neo；离线事务、工具栏、窗口策略和 workspace 门禁已通过，真实桌面与性能验证开放。

## R-031：标注选择/删除命中和 tray-only 窗口隔离缺少原生会话证据（中）

- 证据和影响范围：070 在当前 Overlay 内以图像像素坐标命中最上层标注，Delete/Backspace 通过可撤销删除事务处理，选择框只覆盖呈现帧。Pinora 空闲只能由系统 tray 常驻，Overlay、贴图和任何辅助层不得出现任务栏、Dock 或分页器项。离线单元测试和 `window_policy` 源码守卫只能覆盖代码契约。
- 触发条件：高 DPI、缩放、细描边、相邻或重叠对象、大量对象、连续选择/删除/撤销/重做，以及 Windows/macOS/X11/KDE Wayland 的映射和焦点时序。
- 失败模式：用户感知的命中容差或选中框位置与高 DPI 呈现不符，主线程重绘造成延迟，或既有辅助窗口仍短暂或持续出现在任务栏、Dock 或分页器，违反 tray-only 产品约束。
- 缓解措施和必需验证：保持命中和事务在 core 中确定性计算、选择高亮不参与导出、选择索引在文档变更时失效、建窗只经 `window_policy`；在四类原生桌面验证每个标注类型、重叠对象、Delete/Backspace、undo/redo、清空、重选、取消/退出、任务栏/Dock/分页器、tray、焦点、首帧和帧时间。
- 回滚或隔离动作：移除 `Select` 工具、选择呈现和删除事务；保留既有标注、撤销/重做、清空、导出、tray 生命周期和窗口策略。
- 负责人和状态：Neo；离线核心/App/窗口策略与 workspace 门禁已通过，真实桌面与性能验证开放。

## R-032：标注移动预览与 tray-only 窗口隔离缺少原生会话证据（中）

- 证据和影响范围：071 将选中标注的拖动预览留在当前 Overlay 内存呈现层，释放时才以原位替换事务更新文档；方向键以 1/10 像素移动选中对象。Pinora 空闲仍只由系统 tray 驻留，Overlay、贴图、历史、设置及任何辅助层均不得出现任务栏、Dock 或分页器项。离线像素、事务和 `window_policy` 源码守卫只能证明代码边界。
- 触发条件：4K/HiDPI 选区、连续高频拖拽、大量前后叠放标注、选区边缘外拖、Windows/macOS/X11/KDE Wayland 的焦点和窗口映射时序，或合成器忽略/延迟任务栏、Dock、分页器隔离请求。
- 失败模式：对象坐标或选择框在缩放后偏移，连续预览仍可出现可感知延迟或内存峰值；辅助窗口可能短暂或持续出现在任务栏、Dock 或分页器，违反 tray-only 产品约束。
- 缓解措施和必需验证：维持按 `{源选区, revision, 索引}` 的前缀缓存、不可变原始像素源、释放时单事务、无新窗口路径和唯一 `window_policy` 展示入口；在 Windows、macOS、X11、KDE Wayland 原生会话验证空闲 tray、全部辅助窗口、1x/2x DPI、各标注类型、越界拖动、方向键/Shift、undo/redo、删除/清空、取消、首帧、焦点、任务栏/Dock/分页器与帧时间。
- 回滚或隔离动作：移除 `Replace` 事务、拖拽状态与替换预览，保留选择、删除、清空、既有草稿预览、导出、tray 生命周期和窗口策略。
- 负责人和状态：Neo；离线核心/App/窗口策略与 workspace 门禁已通过，真实桌面与性能验证开放。

## R-033：选区热区调整与 tray-only 窗口隔离缺少原生会话证据（中）

- 证据和影响范围：072 在当前 Overlay 中绘制并命中选区八个调整热区，核心选择模型限制 bounds、最小尺寸和反向拖动；有草稿或已提交标注时不进入该输入路径。Pinora 空闲仍只由系统 tray 驻留，Overlay、贴图、历史、设置及任何辅助层均不得出现任务栏、Dock 或分页器项。离线几何、源码窗口守卫和测试不能观察真实桌面。
- 触发条件：4K/HiDPI、极窄选区、快速连续拖拽、工具栏邻近热区、焦点切换，以及 Windows/macOS/X11/KDE Wayland 的输入缩放、映射和窗口管理器时序。
- 失败模式：热区在真实缩放下可能难以发现或命中到相邻方向，连续拖动仍可能出现帧延迟；既有辅助窗口可能短暂或持续出现在任务栏、Dock 或分页器，违反 tray-only 产品约束。
- 缓解措施和必需验证：维持核心唯一几何规则、最近热区命中、2 像素/32ms 节流、标注/草稿保护和唯一 `window_policy` 展示入口；在 Windows、macOS、X11、KDE Wayland 原生会话验证空闲 tray、1x/2x DPI、八方向、最小选区、边界、快速拖动、标注后重选、取消/完成、焦点、帧时间和任务栏/Dock/分页器。
- 回滚或隔离动作：移除 `SelectionHandle`、热区绘制和拖动分支，恢复既有重新拖选与键盘移动；不影响截图、标注、导出、OCR、贴图、tray 生命周期或窗口策略。
- 负责人和状态：Neo；离线核心/App/窗口策略与 workspace 门禁已通过，真实桌面与性能验证开放。

## R-034：选区物理像素读数与 tray-only 窗口隔离缺少原生会话证据（中）

- 证据和影响范围：073 在当前 Overlay 帧中显示 `W… H… X… Y…`。尺寸来自当前源图选区，坐标经既有缓冲到源图映射后叠加 `display_origin`；面板优先位于选区上方并避让下方工具栏，新旧 panel 都纳入脏区恢复。Pinora 空闲仍只能由系统 tray 常驻，Overlay、贴图、历史、设置及任何辅助层均不得进入任务栏、Dock 或分页器项。离线模型、像素、源码守卫和测试只能证明代码边界。
- 触发条件：4K/HiDPI、极小选区、快速连续拖选或八方向调整、负坐标多显示器、Windows/macOS/X11/KDE Wayland 的输入/映射/窗口管理器时序。
- 失败模式：读数在真实缩放下可能难以辨认、与工具栏/选区视觉冲突、连续更新造成可感知延迟或出现残影；既有辅助窗口可能短暂或持续出现在任务栏、Dock 或分页器，违反 tray-only 产品约束。
- 缓解措施和必需验证：维持源图物理像素映射、纯布局、旧 panel 脏区恢复、现有拖动节流和唯一 `window_policy` 展示入口；在 Windows、macOS、X11、KDE Wayland 原生会话验证空闲 tray、1x/2x DPI、普通/反向拖选、八方向调整、负 origin 多显示器、重选、取消、首帧、焦点、帧时间以及任务栏/Dock/分页器。
- 回滚或隔离动作：移除读数模块与其 Overlay 调用，恢复原有选区边框、热区和工具栏；不影响截图、标注、导出、OCR、贴图、tray 生命周期或窗口策略。
- 负责人和状态：Neo；离线模块/App/窗口策略与 workspace 门禁已通过，真实桌面与性能验证开放。

## R-035：多行文本输入与 tray-only 窗口隔离缺少原生会话证据（中）

- 证据和影响范围：074 在现有 Overlay 文本草稿中支持 `Shift+Enter` 换行，`Enter`/`Ctrl+Enter` 提交，且在外部重选或工具切换前提交非空草稿。core 的文本绘制、无字体 fallback、bounds 和选择命中共用行距，空白行保留垂直空间。Pinora 空闲仍只能由系统 tray 常驻，Overlay、贴图、历史、设置及任何辅助层均不得进入任务栏、Dock 或分页器项。离线文本、事务和源码守卫只能证明代码边界。
- 触发条件：复杂 CJK/Emoji/组合字符、输入法候选、长文本、1x/2x DPI、快速连续输入、多显示器焦点切换，或 Windows/macOS/X11/KDE Wayland 的窗口映射与输入时序。
- 失败模式：真实字体 fallback 或输入法提交可能与离线字体模型不同，行距、bounds、光标、命中或连续重绘可能出现偏差/延迟；既有辅助窗口可能短暂或持续出现在任务栏、Dock 或分页器，违反 tray-only 产品约束。
- 缓解措施和必需验证：维持 content 中显式换行、单一行距、非空草稿单事务提交、空白草稿无 revision、预览缓存和唯一 `window_policy` 展示入口；在 Windows、macOS、X11、KDE Wayland 原生会话验证中英文/CJK/Emoji、输入法、Shift+Enter/Enter/Esc、重选、工具切换、undo/redo、选择/移动、100%/200% 缩放、焦点、帧时间、tray 与任务栏/Dock/分页器。
- 回滚或隔离动作：移除换行和草稿提交边界，恢复既有单行文本逻辑；不改变既有标注文档、截图、贴图、导出、OCR、tray 生命周期或窗口策略。
- 负责人和状态：Neo；离线 core/App/窗口策略与 workspace 门禁已通过，真实桌面与性能验证开放。

## R-036：最近关闭贴图恢复与 tray-only 窗口隔离缺少原生会话证据（中）

- 证据和影响范围：075 的 tray 恢复只保存内存快照，恢复使用新 PinId/asset/window 并在关闭时取消旧 owner；离线测试和窗口策略守卫不能观察真实 tray 菜单、窗口映射、首帧或任务栏/Dock/分页器。
- 触发条件：多贴图连续关闭、恢复时达到 Pin 上限、窗口创建失败、慢速大图、Windows/macOS/X11/KDE Wayland 的 tray/合成器时序。
- 失败模式：恢复入口可能不可用、首帧闪烁、焦点异常或辅助窗口进入任务栏/Dock/分页器；静态门禁不能观测这些行为。
- 缓解措施和必需验证：保持旧 owner 关闭、新 identity、失败保留快照和唯一 `window_policy` 展示入口；在原生会话验证关闭/恢复/失败重试、多个贴图、任务栏/Dock/分页器、tray、首帧、焦点与帧时间。
- 回滚或隔离动作：移除 tray 撤销项和内存快照，恢复既有关闭语义。
- 负责人和状态：Neo；离线门禁通过，真实桌面验证开放。

## R-037：贴图无边框比例缩放与 tray-only 窗口隔离缺少原生会话证据（中）

- 证据和影响范围：076 在既有 Pin 窗口内实现八方向等比缩放、平台 resize 指针和双击/客户区菜单 `100%`。纯尺寸逻辑、缓存失效、transform 同步和 `window_policy` 守卫均可离线验证；它们不能观察真实无边框窗口协议、合成器映射或任务栏/Dock/分页器。
- 触发条件：连续边/角拖动、极小或 4K/HiDPI 图片、OCR Ctrl+拖选、锁定切换、Wayland configure、Windows/macOS/X11/KDE Wayland 的窗口管理器时序。
- 失败模式：平台原生 resize 可能忽略比例收敛或造成左/上锚点漂移、首帧或焦点异常；手动回退可能在特定合成器中无定位能力；快速 resize 仍可能导致可感知重绘延迟，既有辅助窗口也可能进入任务栏、Dock 或分页器。
- 缓解措施和必需验证：保持原生路径不重复设置位置、仅在 native 不可用时调整锚点、仅在实际变化时失效缓存/同步 transform，并继续通过唯一 `window_policy` 展示入口；在 Windows、macOS、X11、KDE Wayland 原生会话验证八方向、最小/最大/反向拖动、100% 恢复、锁定、OCR、滚轮、多贴图、4K/HiDPI、焦点、帧时间、tray 和任务栏/Dock/分页器。
- 回滚或隔离动作：移除尺寸热区、native/manual resize 与 `100%` 菜单动作，恢复原有贴图拖动和滚轮缩放；不影响 PinId、图像、OCR、导出、历史、tray 生命周期或窗口策略。
- 负责人和状态：Neo；离线模块、应用、窗口策略与 workspace 门禁已通过，真实桌面验证开放。

## R-038：贴图初始避让的位置映射与 tray-only 窗口隔离缺少原生会话证据（中）

- 证据和影响范围：077 仅在普通 Overlay 的新贴图动作中，根据当前完整捕获范围以右、左、下、上顺序选择不与来源选区相交且完整容纳的初始物理像素坐标；无候选时受控回退。纯布局和 `window_policy` 守卫可证明算法与代码边界，不能观察真实窗口管理器最终位置。
- 触发条件：窗口截图、全屏来源选区、异构缩放或负坐标多显示器、超大贴图，以及 Windows、macOS、X11、KDE Wayland 对无边框置顶窗口初始位置、映射与工作区边界的不同解释。
- 失败模式：在捕获范围不足时贴图仍会遮挡来源区域，这是明确的回退而非伪造避让；平台也可能忽略、夹取或延迟初始位置，造成与离线物理坐标不同的视觉位置、首帧/焦点异常，或既有辅助窗口进入任务栏、Dock 或分页器。
- 缓解措施和必需验证：保留完整容纳、不相交、负坐标、溢出和回退的纯测试；不推测其他显示器，不新增建窗/展示路径，并继续使用唯一 `window_policy` 展示入口。在 Windows、macOS、X11、KDE Wayland 原生会话验证普通/全屏/窗口截图、正负 origin 多显示器、100%/200% 缩放、无候选回退、首帧、焦点、tray 与任务栏/Dock/分页器。
- 回滚或隔离动作：移除默认位置策略，恢复来源区域左上坐标；不影响图像、PinId、OCR、导出、历史、tray 生命周期或窗口策略。
- 负责人和状态：Neo；离线布局、应用、窗口策略与 workspace 门禁已通过，真实桌面验证开放。

## R-039：tray 异步状态的原生可见性与刷新时序未验证（中）

- 证据和影响范围：078 在既有 `tray-icon` 菜单增加禁用状态项，并以相同的受控
  文案更新图标 tooltip；桌面壳只在截图、延时截图、OCR、导出已启动或经
  owner/`AssetRef` 验证的结果后调用更新。离线测试覆盖文案脱敏、失败不伪装成功、
  陈旧错误丢弃和既有窗口策略，不能观察系统 tray。
- 触发条件：Linux StatusNotifier/AppIndicator、Windows 通知区域或 macOS 菜单栏
  后端忽略或延迟动态菜单文字/tooltip，或用户快速连续发起操作使最新状态在视觉上
  不易分辨。
- 失败模式：用户可能只看到旧状态、看不到 tooltip 或菜单重绘延迟；功能不得因此
  创建通知、Toast、诊断窗口或额外事件循环，且辅助窗口仍可能受既有平台策略影响。
- 缓解措施和必需验证：保留菜单状态项作为 tooltip 的可见退路，状态更新不阻塞
  业务路径；在 Linux、Windows、macOS 原生会话验证快速连续截图、延时取消、
  OCR/导出成功失败、tray 菜单与 tooltip、空闲 tray、Overlay/贴图/历史/设置的
  任务栏/Dock/分页器行为和焦点。
- 回滚或隔离动作：移除 `TrayFeedback`、状态菜单项和桌面壳调用，保留现有截图、
  OCR、导出、延时截图、tray 生命周期与窗口策略。
- 负责人和状态：Neo；离线模型、任务服务、窗口策略和 workspace 门禁已验证，
  原生桌面证据开放。

## R-040：tray 启动能力摘要的原生可见性与实时性未验证（中）

- 证据和影响范围：079 在已有 tray 菜单中增加禁用的能力标题和截图、全局热键、
  系统图像剪贴板、本地 OCR 四条启动摘要；热键使用 `GlobalHotkeyHub` 的实际注册
  结果，其余仅复用已有快照或无进程 PATH 检查。纯模型和 workspace 门禁可证明
  标签脱敏、热键覆盖和无新建窗路径，不能观察系统 tray 或权限 API。
- 触发条件：Linux StatusNotifier/AppIndicator、Windows 通知区域或 macOS 菜单栏
  隐藏、折叠或延迟重绘禁用项；启动后安装/删除依赖；Wayland/X11/Windows/macOS
  的真实热键、剪贴板、OCR 与权限状态和快照不一致。
- 失败模式：用户可能无法在 tray 中看到摘要，或看到启动时的陈旧能力状态；功能
  不得为此创建诊断窗口、通知、worker、外部进程或权限绕过路径，辅助窗口仍可能
  受已有任务栏/Dock/分页器合成器限制影响。
- 缓解措施和必需验证：保持菜单只读且不影响截图/IPC/手动 tray 动作；每次进程
  启动重新组装快照。在 Linux、Windows、macOS 原生会话验证热键成功/失败、截图
  权限、系统剪贴板、tesseract 有/无、菜单可见性/读屏、空闲 tray 与所有辅助窗口
  的任务栏/Dock/分页器、焦点和首帧。
- 回滚或隔离动作：移除 `TrayCapabilitySummary`、菜单标题/条目和构造传参；保留
  现有 tray 状态反馈、截图、热键、OCR、剪贴板和窗口策略。
- 负责人和状态：Neo；离线纯模型、tray 调用、窗口策略和 workspace 门禁已验证，
  原生桌面证据开放。

## R-041：导出名称候选检查与跨进程文件竞态（低）

- 证据和影响范围：080 的桌面壳会在既有导出目录中以 UTC 名称和进程内序号选择不存在的 PNG 候选，再交给原有原子写入 worker；单实例避免第二个 Pinora 同时分配，但不能控制其他本地进程。
- 触发条件：第三方在候选 `try_exists` 后、worker 原子发布前创建同名目标，或用户在运行中修改导出目录内容。
- 失败模式：既有原子发布的替换语义可能覆盖第三方刚创建的同名文件；本任务只保证常见顺序操作和启动时已存在文件的递增，不声明跨进程无覆盖。
- 缓解措施和必需验证：保持有限候选、单实例、受管目录与历史白名单；后续若增加覆盖策略，应在 worker 内实现原子 no-replace 发布并建立跨平台文件系统测试。
- 回滚或隔离动作：恢复内部 `ImageId.png` 命名；不改变现有 worker、历史、截图、贴图或窗口策略。
- 负责人和状态：Neo；离线命名、导出、workspace 门禁已验证，跨进程竞态开放。

## R-042：跨平台全局热键仍缺原生桌面证据（高）

- 证据和影响范围：081 将 `GlobalHotKeyManager` 的构造、持有、轮询和析构收敛到 `DesktopApp` 所在的 `winit` GUI 线程；F2/Ctrl+N 为必需注册，Ctrl+Shift+S/F3 逐项可选，tray 摘要读取实际 `GlobalHotkeyHub` 结果。Windows target 编译与离线事件过滤测试已通过。
- 触发条件：Windows 热键冲突、隐藏 `WS_EX_TOOLWINDOW` 消息窗口的任务栏隔离、macOS 辅助功能/输入监控权限、主 run loop 行为、Linux X11 会话切换或休眠恢复，及纯 Wayland 没有 XDG GlobalShortcuts Portal adapter。
- 失败模式：用户可能只能看到不可用状态、热键不触发、可选组合缺失，或 Windows 依赖内部窗口意外显示；不得把 manager 存活、CI 或 target 编译宣称为系统热键已经可用，更不得以后台键盘监听绕过权限。
- 缓解措施和必需验证：保留 tray 和 `pinora capture` IPC；在 Windows、macOS、Linux X11 与 KDE Wayland 原生会话分别验证注册成功/冲突、触发、退出解除、睡眠恢复、tray-only 任务栏/Dock 状态和 Portal 降级。后续热键录制、冲突建议、热更新和 Portal 必须单独实现与验证。
- 回滚或隔离动作：恢复 Linux 专用热键分支或关闭全局热键入口；tray、IPC、截图、窗口策略和数据格式保持不变。
- 负责人和状态：Neo；离线生命周期、workspace 门禁与 Windows target 已验证，原生桌面证据开放。

## R-043：OCR 语言预设的模型可用性与设置交互仍缺原生桌面证据（中）

- 证据和影响范围：082 将 `Auto`、`English`、`SimplifiedChinese` 写入固定长度 schema v2 设置记录；v1 读取保留旧字段并以 `Auto` 迁移。桌面壳在创建 OCR worker 前冻结 runtime 预设，自动模式只组合本机 `chi_sim`/`eng`，指定模式缺失时返回 `CapabilityUnavailable`；离线 codec、面板、解析和 worker 冻结测试已通过。
- 触发条件：目标系统未安装 `tesseract`、语言包名称或路径与预期不同、`--list-langs` 受权限或环境影响，或真实设置窗口在高 DPI、输入法、键盘焦点和不同主题下不可用。
- 失败模式：用户可保存一个在当前机器不可执行的预设，随后 OCR 受控失败；这比隐式回退安全，但仍可能造成使用摩擦。真实 Tesseract stderr 不能作为日志或 tray 文本输出，以免泄露路径或外部内容。
- 缓解措施和必需验证：保持无下载、无任意 CLI 参数、无跨语言回退和稳定错误码；在 Windows、macOS、X11、KDE Wayland 分别验证三种预设的保存、重启、可用/缺失模型、取消、超时、词框、文本复制、tray 提示、设置焦点和任务栏/Dock/分页器隔离。
- 回滚或隔离动作：读取 v2 仍保留，提交时忽略 `ocr_language` 并统一使用 `Auto`；不改变 v1 数据、设置原子写入、OCR 监督、历史、导出或窗口策略。
- 负责人和状态：Neo；离线设置、服务和严格 workspace 门禁已验证，真实模型与桌面证据开放。

## R-044：OCR 内存缓存的真实占用与外部模型变化未验证（中）

- 证据和影响范围：083 的 `OcrJobService` 只缓存 accepted 成功结果，键为完整 `AssetRef` 和冻结 `OcrLanguage`；失败、取消、owner 关闭、超时和陈旧结果不写入。缓存最多 8 条、累计估算 2 MiB、单条估算 512 KiB，命中不创建新的 Tesseract worker，进程退出即清空。
- 触发条件：4K/长文本 OCR 的实际 `String`/`Vec` 分配明显大于估算，用户短时间处理大量不同资产，或运行中替换本机 Tesseract/语言模型但仍命中已有内存结果。
- 失败模式：常驻进程的真实峰值可能超过估算；在外部模型更新前得到的结果可在相同 asset/language 下继续复用到进程退出。这不影响资产 generation 或语言变更的隔离，但不能宣称对模型文件变化即时失效。
- 缓解措施和必需验证：维持条目和估算字节上限、过大结果不缓存、只缓存 accepted 成功和进程退出释放；在 Windows、macOS、X11、KDE Wayland 测量连续点击、不同语言、编辑 generation 变化、取消、长文本/4K 内存、词框刷新、全文复制、tray 和任务栏/Dock/分页器。模型/引擎配置可变时应在独立任务中扩展 cache key 或提供显式清理。
- 回滚或隔离动作：删除 `OcrResultCache` 和 `start_or_reuse`，恢复每次 OCR 都走既有受监督 worker；不改变 OCR 语言、设置、资产、历史、导出或窗口策略。
- 负责人和状态：Neo；离线缓存边界、服务回归和 workspace 门禁已验证，真实性能与模型变化证据开放。

## R-045：可配置全局热键的原生注册与恢复尚未验证（高）

- 证据和影响范围：084 将设置 schema 升级至 v3，持久化区域和全屏主键；v1/v2 自动以 F2/F3 迁移。设置窗口在现有辅助窗口内录制受限物理键，录制优先于本窗口截图快捷键；`GlobalHotkeyHub` 先注册全部新键、失败时释放已注册的新键并保留旧动作映射，撤销旧键失败时尝试恢复已撤销旧键。保存失败时桌面壳尝试重绑回先前配置；086 只在设置成功提交后把已保存组合原地同步到 tray 菜单，Ctrl+N/Ctrl+Shift+S、tray 与 IPC 仍是独立区域截图入口。
- 触发条件：Windows/macOS/Linux X11 的系统冲突、权限、键盘布局、休眠恢复、窗口焦点、热键 API 对同进程临时重复注册的限制，或 Wayland 没有 XDG GlobalShortcuts Portal adapter。
- 失败模式：设置可能显示已保存但原生后端在下一次会话拒绝组合，重绑或回滚中的 OS 级部分失败可能使个别键暂时不可用；纯 Wayland 仍不会注册全局键。tray 标签只表示已保存配置，可能与实际注册状态不同；不得把 fake registrar、target 编译、CI 或标签重绘当成真实系统注册、任务栏/Dock 或焦点行为的证据。
- 缓解措施和必需验证：保留初始注册实际结果、受控不可用状态、tray 与 `pinora capture` IPC；在 Windows、macOS、Linux X11、KDE Wayland 分别验证录制安全键/冲突键、保存、重启、重绑失败、磁盘写入失败、退出解除、休眠恢复、焦点、任务栏/Dock/分页器与 Portal 降级。平台重绑事务若显示 API 语义差异，按平台适配而不是引入后台键盘监听。
- 回滚或隔离动作：忽略 v3 两个热键字段并恢复固定 F2/F3 主键；保留 v1/v2 读取、Ctrl+N/Ctrl+Shift+S、tray、IPC、截图、贴图与窗口策略。
- 负责人和状态：Neo；领域/codec/录制/重绑成功和失败的离线契约已验证，原生桌面证据开放。

## R-046：诊断能力粒度与原生窗口反馈尚未验证（中）

- 证据和影响范围：085 已从 tray 打开本地诊断 `Panel`，内容只读取平台常量、能力布尔值、`GlobalHotkeyHub` 实际注册、`tesseract_available` 与 `TrayFeedback` 固定枚举；模型不保留 `CapabilitySnapshot.notes`，失败只显示稳定错误码和固定建议。
- 触发条件：不同桌面环境的 tray 菜单、辅助窗口任务栏/Dock/分页器策略、焦点、读屏或系统权限状态与 bootstrap 布尔值不一致，或者用户需要更细的后端、权限范围、探测时间和诊断包信息。
- 失败模式：面板可能只能显示安全但粗粒度的“受限”，或原生窗口仍意外可见；不得为了更详细反馈而显示原始错误、路径、截图、OCR/剪贴板内容，也不得把离线测试或 target 编译称为原生桌面验收。
- 缓解措施和必需验证：保持 tray/IPC 可用和固定脱敏映射；在 Windows、macOS、Linux X11、KDE Wayland 验证打开/关闭/焦点、任务栏/Dock/分页器、读屏、能力变化、权限拒绝和失败建议。需要完整能力快照、复制详情或诊断包时，先单独设计版本化脱敏结构、用户确认和无内容泄漏测试。
- 回滚或隔离动作：移除诊断 tray 入口及 `diagnostics_*` 内部模块；现有 tray 反馈、能力摘要、窗口策略、截图、贴图、OCR、设置、历史与 IPC 不变。
- 负责人和状态：Neo；离线内容、tray action、window policy、workspace 门禁与 Windows target 已验证，原生桌面证据和完整诊断范围开放。

## R-047：默认贴图置顶的原生窗口层级与 tray-only 隔离尚未验证（中）

- 证据和影响范围：088 将 `default_pin_always_on_top` 写入 schema v4，并在设置原子回读成功后仅影响后续新贴图的领域状态和 `WindowLevel` 请求；v1/v2/v3 迁移保持既有默认置顶，关闭后恢复贴图保留快照层级。设置窗口仍由既有 `window_policy` 隐藏创建和唯一展示入口管理。
- 触发条件：Windows、macOS、X11、KDE Wayland 或其他 Wayland 合成器忽略、延迟处理或重排 `WindowLevel::Normal`/`AlwaysOnTop`；窗口映射、KWin 定位、焦点转移、缩放或任务栏/Dock/分页器策略与离线模型不同。
- 失败模式：用户保存 `OFF` 后新贴图仍可能被窗口管理器提升，保存 `ON` 后也可能无法保持置顶；辅助窗口还可能短暂或持续出现在任务栏、Dock 或分页器。codec、target 编译和单元测试不能证明真实窗口层级或交互流畅性。
- 缓解措施和必需验证：保持层级只在成功保存后的新建路径请求，避免改动既有贴图或新增窗口；在 Windows、macOS、Linux X11、KDE Wayland 的 100%/200% 缩放下分别验证 `OFF`/`ON` 保存、重启、连续创建、手动层级切换、关闭恢复、Overlay 过渡、焦点、tray 空闲和任务栏/Dock/分页器。
- 回滚或隔离动作：保留 v4 decoder，忽略 `default_pin_always_on_top` 并恢复新贴图固定置顶；不改变已打开贴图、手动层级菜单、设置原子写入、tray 生命周期、截图、历史、OCR 或窗口策略。
- 负责人和状态：Neo；离线 codec、面板、状态传播、窗口策略、workspace 严格门禁与 Windows target 编译已验证，原生桌面证据开放。

## R-048：动态 tray 贴图列表、最近使用排序与单贴图焦点的原生行为尚未验证（中）

- 证据和影响范围：089 为既有系统 tray 增加可动态重建的“贴图列表”子菜单；090 令其按进程内最近使用排序。条目只展示通用序号和可见性；空列表为禁用占位。激活条目仅命中当前 `PinWin`，经 `window_policy::show_auxiliary_window` 显示、聚焦和重绘；新建、`Focused(true)` 和 tray 唤起只推进内存饱和计数，未新建窗口、线程、截图或持久化。创建、关闭、批量显示/隐藏、延时截图隐藏/恢复及编辑可见性变化均同步列表。
- 触发条件：Windows/macOS/Linux X11/KDE Wayland 的系统 tray 对子菜单动态 remove/append 刷新有延迟或失败，系统焦点事件缺失/延迟，菜单事件在贴图关闭后到达，或窗口管理器拒绝/延迟焦点、任务栏/Dock/分页器隔离请求。
- 失败模式：列表可能短暂显示旧可见性或非预期的最近使用顺序；点击陈旧条目会被安全忽略，但用户无法立即定位贴图；已存在贴图的聚焦可能不生效，或辅助窗口仍短暂/持续出现在任务栏、Dock 或分页器。离线菜单映射、静态策略守卫、target 编译和 CI 不能证明原生体验或流畅性。
- 缓解措施和必需验证：保留 GUI 线程更新、失败仅记录且不触碰 Pin 状态、recency 降序/PinId 回退、饱和计数、无内容标签、未知 `PinId` 忽略和唯一 `window_policy` 展示入口；在 Windows、macOS、Linux X11、KDE Wayland 的 100%/200% 缩放分别验证空/多贴图、创建/关闭、焦点切换、tray 唤起、显示/隐藏、延时截图、编辑取消/提交、陈旧菜单点击、首帧、tray 空闲和任务栏/Dock/分页器。
- 回滚或隔离动作：删除 recency 字段/排序和动态子菜单、`ActivatePin` 分支及刷新调用；既有贴图、显示/隐藏/关闭全部、最近关闭撤销、截图、历史、OCR、设置和窗口策略不变。
- 负责人和状态：Neo；离线 tray 映射/标签/排序/饱和计数、桌面壳和窗口策略门禁已验证，原生桌面证据开放。

## R-049：OCR 阈值的模型语义与原生词框可读性尚未验证（中）

- 证据和影响范围：091 在 schema v5 中持久化 0..=100 的 OCR 置信度阈值，默认 60；v1-v4 均以默认值迁移，保存以原有原子回读为门槛。贴图只在既有窗口内将未选中、已知且严格低于阈值的词框绘制为告警色；`-1`、NaN、无穷和越界置信度保持普通样式，选中词保持选中样式。OCR 原始结果、全文/框选复制、缓存和 worker 不会因阈值改变而重跑或删词。
- 触发条件：不同 Tesseract 版本、语言、模型和图像类型对 confidence 的标度或可靠性不同，或 Windows/macOS/X11/KDE Wayland 的 HiDPI、色彩、softbuffer、焦点和合成器调度使告警描边不易辨认或造成连续重绘延迟。
- 失败模式：用户可能将相对置信度误认为文本正确性结论，或在真实缩放/主题下难以区分低置信与普通/选中框；辅助窗口仍可能受到既有任务栏/Dock/分页器隔离风险影响。离线状态、codec、target 编译和 CI 不能证明这些原生体验。
- 缓解措施和必需验证：保持阈值仅作可逆视觉提示、0..=100 边界、未知值中性、选中优先、成功保存后才重绘和无新增窗口/worker；在 Windows、macOS、Linux X11、KDE Wayland 的 100%/200% 缩放下验证英中模型、低/等于/高阈值、未知值、Ctrl+拖选、全文/框选复制、保存失败、连续贴图、焦点、首帧、帧时间、tray 空闲和任务栏/Dock/分页器。
- 回滚或隔离动作：移除设置面板入口、保存后的 redraw 和词框告警状态即可恢复普通/选中词框；保留 v5 decoder 或忽略其值，不影响 OCR、设置文件可读性、贴图、tray 或窗口策略。
- 负责人和状态：Neo；离线核心/codec/面板/呈现、严格 workspace 门禁与 Windows target 编译已验证，模型和原生桌面证据开放。

## R-050：多格式文件编码的真实查看器兼容性与性能未验证（中）

- 证据和影响范围：092 在既有受监督导出 worker 中新增 PNG、JPEG 与无损 WebP；格式和 JPEG 质量在提交前冻结，路径扩展名必须匹配冻结格式。PNG/WebP 写入 RGBA，JPEG 以整数运算确定性白底合成为 RGB；全部格式复用同目录临时文件、同步、原子替换、owner/generation/截止时间门禁。设置 schema v6 保存格式与 1..=100 JPEG 质量，v1-v5 默认迁移 PNG/90。系统剪贴板仍为 PNG，历史仍只索引受管 PNG。
- 触发条件：不同 Windows/macOS/Linux 文件查看器或分享目标对 JPEG/WebP 解码、色彩、ICC/EXIF 缺失、透明像素白底合成、极端 JPEG 质量、大分辨率图像、慢速磁盘或受限空间的行为不同；以及原生 tray 刷新、焦点、HiDPI 和辅助窗口任务栏/Dock/分页器隔离时序。
- 失败模式：离线魔数、编码和原子写入测试通过，但实际查看器可能出现色彩差异、JPEG 透明边缘视觉差异、WebP 不被目标应用接受，或大图编码占用 CPU/内存导致可感知延迟；非 PNG 文件不会进入现有历史，用户只能通过本次保存路径访问。tray-only 窗口策略没有因该任务新增窗口，但真实合成器仍可能不满足隔离请求。
- 缓解措施和必需验证：保持最小 `image` JPEG/WebP 特性、worker 编码、路径/格式提交前校验、静态托盘文案、PNG-only 历史隔离和无敏感日志；在 Windows、macOS、Linux X11、KDE Wayland 分别以透明/半透明/不透明、小图/大图、JPEG 1/90/100、PNG/WebP 打开与导入、慢盘/空间不足、取消/关闭 owner、tray 状态、100%/200% 缩放、焦点和任务栏/Dock/分页器进行原生验证并记录帧时间和内存。
- 回滚或隔离动作：隐藏格式/质量设置并使新 `SaveImage` 只提交 PNG，保留 v6 decoder 读取；既有 PNG、剪贴板、历史、任务门禁和窗口策略不变。
- 负责人和状态：Neo；定向离线编码、codec、面板、命名、任务、历史和 tray 反馈测试已通过，真实查看器、性能与原生桌面证据开放。

## R-051：Overlay 导出来源的原生可发现性与结束帧时间未验证（中）

- 证据和影响范围：093 在既有 Overlay 客户区工具栏增加 `RAW`/`ANN` 来源状态；默认 `ANN`，Copy/Save 在结束 Overlay 前从当前选区生成原图或标注合成 RGBA 并提交既有受监督 worker。贴图与 OCR 固定使用合成图；贴图 Copy/Save 只克隆 `PinWin.image`，没有窗口或桌面表面捕获，也不会烧录缩放、透明度、OCR 词框或客户区 UI。
- 触发条件：4K/HiDPI 大选区、复杂标注、连续切换/复制/保存、慢速剪贴板或磁盘，以及 Windows/macOS/X11/KDE Wayland 的软缓冲重绘、焦点、窗口映射和任务栏/Dock/分页器策略与离线模型不同。
- 失败模式：用户可能难以在高 DPI 或窄选区中辨识 `RAW`/`ANN` 状态；Overlay 收尾仍需要同步生成一次图像，实际主线程延迟可能可感知。即使本任务没有新增窗口，既有辅助窗口仍可能短暂或持续显示在任务栏、Dock 或分页器。离线像素、目标编译和 CI 不能证明真实剪贴板、查看器、流畅性或窗口管理器结果。
- 缓解措施和必需验证：保持来源状态只在当前 Overlay 自绘，Copy/Save 只选择一条既有图像路径，使用原有 worker、owner/generation/截止时间门禁，禁止新增窗口捕获或额外缓存；在 Windows、macOS、Linux X11、KDE Wayland 的 100%/200% 缩放下验证 RAW/ANN、含/不含标注、草稿提交、双击复制、文件保存、Pin、OCR、连续操作、帧时间、tray 空闲、焦点和任务栏/Dock/分页器。
- 回滚或隔离动作：移除来源枚举、工具栏控件和选择分支，使 Overlay Copy/Save 重新固定合成图；贴图、OCR、导出 worker、格式、历史、设置和窗口策略保持不变。
- 负责人和状态：Neo；离线工具栏、像素、动作路由、严格 workspace 门禁与 Windows target 已验证，原生桌面证据开放。

## R-052：可取消文件保存的慢盘响应、原子发布竞态与原生 tray 刷新未验证（中）

- 证据和影响范围：094 在已有 `ExportJobService` 上提供单 `JobId` 协作式取消，DesktopApp 只从运行中的 `PendingExportAction::SaveImage` 收集取消目标。保存 worker 在编码前、编码后、`sync_all` 后且原子 `rename` 前检查 token；发布前取消依赖 `AtomicExportTemp` 的 Drop 清理当前临时文件。取消后 pending 映射保留至 worker terminal，tray 用固定“正在取消文件保存”/“文件保存已取消”反馈；CopyImage、CopyText、OCR、截图和其他 owner 不在此入口中。
- 触发条件：4K 或更大图像的编码、慢速/满载/网络挂载磁盘、`sync_all` 阻塞、取消恰好落在最后检查和 `rename` 之间，或 Windows/macOS/X11/KDE Wayland 的原生 tray 菜单刷新比 GUI 事件循环慢。
- 失败模式：取消响应只能等到下一个协作检查点；极窄发布竞态可能保留已经原子发布的目标文件，不能伪装为回滚。用户也可能短暂看到过期的 tray 菜单状态；离线测试和交叉编译不能证明真实文件系统原子性、查看器可见性、桌面流畅性或任务栏/Dock/分页器隔离。
- 缓解措施和必需验证：保持取消范围仅为运行中的文件保存、发布前临时文件 RAII 清理、发布后不删除、pending 收敛后才移除和静态脱敏反馈；在 Windows、macOS、Linux X11、KDE Wayland 上以小图/大图、PNG/JPEG/WebP、快盘/慢盘、目标已存在、重复取消、同时复制/OCR、关闭 owner、tray 刷新、100%/200% 缩放、帧时间及任务栏/Dock/分页器进行原生验证。
- 回滚或隔离动作：移除 tray 的“取消文件保存”入口、单任务取消代理和保存检查点，恢复既有 worker 完成或 owner 关闭时取消的行为；保留原子保存、格式/质量、历史、剪贴板、OCR、窗口策略和设置 schema。
- 负责人和状态：Neo；离线保存、导出服务、tray、反馈和桌面壳筛选测试已验证，真实文件系统与原生桌面证据开放。

## R-053：贴图鼠标穿透的原生输入区域、tray 恢复与 tray-only 隔离未验证（中）

- 证据和影响范围：095 仅在现有 `PinWin` 生命周期内保存 `PinMouseMode`。客户区 `PASS` 仅在 `winit::Window::set_cursor_hittest(false)` 返回成功后清除菜单、拖动、缩放、OCR 选择和双击瞬态状态；动态 tray 条目只显示通用序号、可见性与“鼠标穿透”。激活穿透条目先调用 `set_cursor_hittest(true)`，只有成功才通过既有 `window_policy` 展示、聚焦和重绘，失败保持状态且反馈固定 `CapabilityUnavailable`。离线状态、菜单、标签、反馈、静态窗口策略与交叉编译不能观察实际桌面输入路由。
- 触发条件：Windows、macOS、X11、KDE Wayland 或其他 Wayland 合成器对空输入区域、窗口映射、焦点恢复、隐藏贴图和动态 tray 菜单的实现差异；快速连续 tray 点击、贴图关闭后的陈旧菜单、HiDPI、多贴图、拖动/缩放/OCR 选择切换及合成器延迟。
- 失败模式：窗口库调用成功但合成器仍截获鼠标，或恢复后鼠标/焦点不能及时回到贴图；tray 条目可能延迟显示状态。任何辅助窗口也可能短暂或持续出现在任务栏、Dock 或分页器，违反 tray-only 产品约束。离线测试、CI 和 target 编译不能证明输入穿透、流畅性或窗口管理器结果。
- 缓解措施和必需验证：保持平台调用成功为唯一状态门槛、失败不显示/聚焦/推进最近使用、穿透后仅经同一 tray 条目恢复、无内容标签和唯一 `window_policy` 展示入口；在 Windows、macOS、Linux X11、KDE Wayland 的 100%/200% 缩放下验证可见/隐藏/锁定贴图启用穿透、鼠标落点、tray 恢复、重复/陈旧条目、拖动/缩放/OCR 瞬态清理、焦点、首帧、帧时间以及任务栏/Dock/分页器。
- 回滚或隔离动作：移除 `PASS` 菜单、`PinMouseMode`、`set_cursor_hittest` 调用、tray 状态标签与反馈分支，恢复始终可鼠标交互的既有贴图；图像、PinId、设置、历史、关闭恢复、OCR、导出、窗口策略和 tray 生命周期保持不变。
- 负责人和状态：Neo；离线菜单、标签、状态提交、反馈、workspace 严格门禁与 Windows target 已验证，原生桌面证据开放。

## R-054：历史保留期的系统时钟跳变、文件系统中断与桌面刷新未验证（中）

- 证据和影响范围：096 将设置 schema 升级到 v7 并追加 1..=3650 天保留期，默认 30 天；历史领域只比较应用层传入的 Unix 毫秒截止点，桌面启动、设置保存和受管 PNG 新增统一先持久化数量/保留期 tombstone，再清理直属受管 PNG。系统时间不可读取时跳过时间淘汰，索引保存失败恢复内存变更，文件删除或最终压缩失败保留 tombstone；外部路径、嵌套路径和活动同名文件继续受保护。
- 触发条件：用户手动调整系统时钟、NTP 回拨/跳跃、休眠恢复、跨时区迁移、时间读取异常，进程在 tombstone 原子写入和文件删除之间崩溃，慢盘/只读/网络挂载、Windows 文件占用，以及历史窗口正在加载时设置保存或启动清理。
- 失败模式：条目可能比用户预期更早或更晚过期；离线测试无法证明断电后的目录持久性和跨平台删除语义；历史面板可能短暂显示旧列表。不得把 codec、目标编译或 fake 文件测试描述为时钟、GUI 或文件系统生产验证。
- 缓解措施和必需验证：保留严格 `created_at_ms < cutoff` 边界、系统时钟失败时保守跳过、tombstone 两阶段清理、活动同名保护和单次历史面板刷新；在 Windows、macOS、Linux X11、KDE Wayland 分别验证重启/迁移、时钟前后跳变、休眠恢复、慢盘/只读目录、删除失败重试、进程中断恢复、历史加载竞态、100%/200% 缩放、tray 空闲和任务栏/Dock/分页器。
- 回滚或隔离动作：隐藏保留期设置并忽略 `history_retention_days`，恢复仅数量/现有容量策略；保留 v7 decoder 读取但不执行时间淘汰，既有历史索引格式、tombstone、外部导出、设置原子写入和窗口策略不变。
- 负责人和状态：Neo；core/codec/面板/历史事务与离线门禁已验证，真实时钟、断电/文件系统和原生桌面证据开放。

## R-055：历史容量配额的迁移清理与跨平台文件系统语义未验证（中）

- 证据和影响范围：097 将设置 schema 升级至 v8，并追加 16 MiB..=64 GiB 的 `history_max_bytes`，默认 1 GiB；v1-v7 按默认容量迁移，v8 无效容量逐字段修复。启动、设置成功保存和受管 PNG 新增均把该容量与数量、保留期一起交给既有策略协调器；活动索引字节超额时最旧条目先成为持久 tombstone，之后才清理直属受管 PNG。离线 codec、容量淘汰、索引保存失败回滚、受管路径保护和 workspace 门禁只能证明已建模事务。
- 触发条件：旧安装已有超过 1 GiB 的受管历史、用户将配额下调、PNG 的实际文件长度与索引元数据不一致、断电发生在原子索引写入和文件删除之间、慢盘/只读/网络挂载、Windows 文件占用，以及历史窗口正在加载时保存容量设置。
- 失败模式：首次 v8 迁移或容量下调可能按策略删除超额受管历史；离线测试无法证明跨平台 `rename`/删除的断电持久性和文件锁语义，历史面板也可能短暂显示旧列表。不得把 codec、target 编译或 fake 文件测试描述为真实文件系统、GUI 或平台行为验证。
- 缓解措施和必需验证：保持 1 GiB 默认、严格容量边界、最旧优先、先索引 tombstone 后文件清理、索引失败回滚、失败 tombstone 重试、外部/嵌套/活动同名路径保护；在 Windows、macOS、Linux X11、KDE Wayland 分别验证 v7 到 v8 迁移、配额上/下调、超额、缺失/占用/只读文件、进程中断恢复、历史加载竞态、100%/200% 缩放、tray 空闲和任务栏/Dock/分页器。
- 回滚或隔离动作：隐藏容量设置并传入 `u64::MAX` 恢复无限制容量；保留 v8 decoder 读取但不执行容量淘汰，历史索引、tombstone、外部导出、设置原子写入和窗口策略不变。
- 负责人和状态：Neo；离线 core/codec/面板/历史事务、workspace 门禁与 Windows target 已验证，真实文件系统和原生桌面证据开放。

## R-056：GitHub runner 发布 smoke 不等价于真实桌面发行验收（高）

- 证据和影响范围：098 已完成：CI `30783363209`、tag package/release `30783568639` 和 `runtime-verify` `30783727003` 均在 Linux/macOS/Windows runner 成功；`v0.1.0-preview.8` pre-release 的 11 个资产及合并清单已下载并逐项校验，runtime 报告已回写 Release body。workflow 的 pre-release 资产没有新增业务逻辑。
- 触发条件：hosted runner 无用户桌面会话、屏幕捕获授权、真实 tray/热键/窗口管理器，且当前资产未配置 Windows 代码签名或 macOS notarization。
- 失败模式：可下载、可安装和 `--version` 成功的包仍可能在目标用户机器上被 SmartScreen/Gatekeeper 警告，或出现截图权限、tray-only、任务栏/Dock/分页器、Wayland/KWin、HiDPI、焦点和流畅性问题；不能把 package/runtime run 当作完整跨平台产品验收。
- 缓解措施和必需验证：发布说明固定标注 pre-release、未签名/未公证和真实桌面风险；保留同一 package run 的逐平台清单、runtime 报告和 tag 关联。后续必须在授权的 Windows、macOS、Linux X11、KDE Wayland 会话分别验证启动、热键、截图、Overlay、贴图、tray-only、任务栏/Dock/分页器、关闭和帧时间。
- 回滚或隔离动作：若运行时验证失败，保留失败 run 与资产证据，修复后创建新的 preview tag；不得删除或覆盖失败证据来伪造发布成功。
- 负责人和状态：Neo；runner package/runtime smoke 与预发布已验证，真实桌面、签名/公证和性能证据仍开放。

## R-057：脱敏诊断包的原生目录与托盘交互尚未验证（中）

- 证据和影响范围：099 增加用户主动“导出诊断包”托盘动作和 `diagnostics_export` 白名单报告；报告只包含版本、平台、能力布尔状态、稳定错误码、固定反馈枚举与数值/枚举设置摘要，使用同目录临时文件、同步、原子 rename 和可读性校验。离线测试已验证字段排除、可读发布和临时文件清理。
- 触发条件：用户主动导出时 Pinora 管理目录不存在、只读、空间不足、网络挂载或文件被占用；Windows/macOS/Linux tray 后端对新菜单项刷新、点击事件和 tooltip 更新存在差异。
- 失败模式：报告无法发布或托盘菜单短暂显示旧状态；离线测试无法证明真实权限、断电持久性、原生菜单响应和辅助窗口任务栏/Dock/分页器行为。报告模型未来扩展若绕过白名单可能引入敏感字段。
- 缓解措施和必需验证：保持 `SanitizedDiagnosticReport` 只接受固定标签/枚举/数值，写入失败只发固定失败反馈且清理临时文件；在 Windows、macOS、Linux X11、KDE Wayland 进行用户主动导出、只读/空间不足、重复点击、托盘刷新、报告内容脱敏和 tray-only/任务栏/Dock/分页器检查。
- 回滚或隔离动作：移除托盘导出项和 `write_report` 调用，恢复仅诊断面板；不改变能力探测、历史、设置、截图、OCR 或窗口策略。
- 负责人和状态：Neo；报告模型、原子写入和离线门禁已验证，原生托盘/文件系统证据开放。

## R-059：Wayland GlobalShortcuts Portal 的原生后端、授权与触发尚未验证（高）

- 证据和影响范围：100 在 Linux + Wayland 会话门槛通过后启动独立 `pinora-wayland-hotkeys` worker，固定绑定 `capture-region` 与 `capture-full-display`；CreateSession、BindShortcuts、Request 响应和 `Activated` 信号不进入 GUI 事件循环，未知标识、取消、失联和缺失接口只发布稳定能力状态并保留 tray/IPC。Linux target-only 依赖、Portal/Hotkey/Desktop Shell 定向测试、workspace 编译、严格 Clippy、Windows target 编译和全量离线测试已通过；当前开发机只确认 Portal 接口未暴露，未确认授权或真实触发。
- 触发条件：KDE/GNOME/其他 Wayland backend 对 `org.freedesktop.portal.GlobalShortcuts` 版本、父窗口 token、用户授权 UI、preferred trigger 格式、session 生命周期、重新绑定和 `Activated`/`Deactivated` 信号的实现差异；Portal 服务重启、用户拒绝、桌面休眠恢复或 GUI 线程高负载。
- 失败模式：Portal 可能拒绝创建/绑定、等待用户交互、返回部分快捷键、断开后不再触发或触发重复；用户仍应能通过 tray/IPC 截图。即使 D-Bus 状态显示 Available，合成器也可能让热键延迟、与其他应用冲突，或辅助窗口短暂出现在任务栏、Dock、分页器；离线 D-Bus 解析、CI、target 编译和当前机器的缺失接口都不能证明这些原生行为。
- 缓解措施和必需验证：保持两个固定 ID、受限错误码、后台 worker、GUI 非阻塞轮询、重绑先关闭旧 session、失败不破坏既有本地后端/tray/IPC，且所有辅助窗口继续经 `window_policy`。在至少 KDE Wayland 与一个非 KDE Wayland 原生会话验证首次授权/拒绝、两项触发、未知信号过滤、重绑、Portal 重启、休眠恢复、退出清理、tray-only、任务栏/Dock/分页器、焦点、首帧、100%/200% HiDPI 和连续截图帧时间；未通过前不宣称纯 Wayland 热键生产可用或 Snipaste 级流畅度。
- 回滚或隔离动作：移除 `wayland_portal`、Linux target-only 依赖和 `GlobalHotkeyHub` Portal 接线，恢复纯 Wayland 的 tray/IPC 降级；不改变 X11/Windows/macOS 热键、设置 schema、截图、贴图、OCR、导出或窗口策略。
- 负责人和状态：Neo；离线契约与静态门禁已验证，真实 Wayland backend、授权、触发、窗口管理器和性能证据开放。

## R-060：KDE 指定显示器全屏的多屏映射与性能尚未验证（高）

- 证据和影响范围：101 已将 KDE/Spectacle 的 `-m` 快速路径收窄到唯一显示器拓扑；多显示器 `FullDisplay { display }` 改为一次 `-f` 全桌面 PNG，按开始时的目标 `DisplayInfo.bounds` 裁剪。离线决策、全桌面尺寸校验、workspace 严格门禁和 Windows target 编译证明不会再把“当前鼠标所在屏”误报为 tray 选定显示器。
- 触发条件：KDE Plasma/Spectacle 版本、异构缩放、旋转、负坐标、显示器热插拔、屏幕排列空洞、`kscreen-doctor` 拓扑与 `spectacle -f` 像素尺寸/原点不同，或大分辨率全桌面 PNG 的解码/裁剪造成可感知延迟。
- 失败模式：后端可能受控拒绝尺寸不匹配，也可能因真实缩放映射差异裁剪错误区域；多屏指定全屏相比单屏 `-m` 需要更多像素、内存和时间。离线路径测试、CI 和 target 编译不能证明真实 KDE 截图准确性、首帧、tray、焦点、任务栏/Dock/分页器或 Snipaste 级流畅度。
- 缓解措施和必需验证：保持一次原子全桌面快照、严格尺寸验证、目标 bounds 裁剪和失败不产生资产；在 KDE Wayland 与 KDE X11 真实双屏/三屏会话分别验证同分辨率/异构缩放、正负 origin、旋转、热插拔、每个 tray 指定屏、区域/AllDisplays、取消、失败恢复、100%/200% HiDPI、连续截图帧时间、tray-only 与任务栏/Dock/分页器。若不一致，禁用指定显示器入口，不以另一屏或 fake 像素回退。
- 回滚或隔离动作：保留唯一显示器 `-m` 快路径，隐藏 KDE 多显示器指定全屏入口或让其返回 `CapabilityUnavailable`；不改变区域截图、AllDisplays、窗口截图、tray/IPC、热键、设置或数据格式。
- 负责人和状态：Neo；离线决策与静态门禁已验证，真实 KDE 多显示器/性能/窗口管理器证据开放。

## R-061：KDE 显示器探测命令与驱动输出兼容性尚未验证（高）

- 证据和影响范围：102 移除了 `kscreen-doctor` 失败时硬编码 `3840x2160` 占位屏，改为真实执行 `xrandr --query`，只接受 connected 且有正尺寸 geometry 的输出，支持 primary 标记和正负坐标；命令失败、无输出或 malformed geometry 均受控返回 `CapabilityUnavailable`。解析器、workspace 严格门禁、Windows target 和全量离线测试已通过。
- 触发条件：KDE Plasma/KScreen 版本差异、显卡驱动、省电/热插拔时序、xrandr 不存在或无法连接 X server、Wayland 会话没有可用 xrandr、输出名称包含特殊字符、旋转/缩放导致 geometry 与 Spectacle PNG 尺寸不一致。
- 失败模式：真实环境可能暂时失去显示器列表，用户只能使用 tray/IPC 等替代入口；也可能因格式变化被保守拒绝。解析单测、CI 和 target 编译不能证明真实拓扑、权限、截图像素、首帧、HiDPI、任务栏/Dock 或性能。
- 缓解措施和必需验证：保持 `kscreen-doctor` 优先、xrandr 严格解析、无硬编码尺寸、无 fake 生产回退和稳定错误码；在 KDE X11、KDE Wayland（有/无 xrandr）、多屏正负 origin、异构缩放、旋转、热插拔、登录/休眠恢复下验证 displays、区域/指定全屏/AllDisplays、尺寸校验、取消/失败恢复、tray-only、任务栏/Dock/分页器和帧时间。解析不一致时禁用 KDE 后端或增加基于官方接口的独立探针，不猜测几何。
- 回滚或隔离动作：仅保留成功的 `kscreen-doctor` 路径，或完全禁用 KDE 后端并让 xcap/受控不可用接管；不恢复固定虚拟拓扑。
- 负责人和状态：Neo；离线解析与静态门禁已验证，真实命令/驱动/Wayland 行为和桌面性能证据开放。

## R-062：GlobalShortcuts Portal 版本属性与方法完整性尚未验证（高）

- 证据和影响范围：103 将 Portal 最低版本设为 v2；worker 在读取 `version` 后，v1 及更低版本立即发布 `VersionUnsupported`，不执行 CreateSession/BindShortcuts，也不改变 tray/IPC。版本契约、Portal 定向测试、workspace 编译、严格 Clippy、Windows target 和全量离线测试已通过。
- 触发条件：不同 xdg-desktop-portal/backend 可能报告 v2 但缺失某个方法或使用不同 options/signals；用户授权拒绝、backend 重启、升级/降级、信号延迟或桌面会话切换。
- 失败模式：版本门槛通过后仍可能在请求阶段受控失败、只接受部分 shortcut 或失联；不得把版本属性视为真实热键注册。离线版本测试、CI、target 编译和当前机器缺失接口不能证明真实授权、触发、冲突、tray-only、任务栏/Dock 或性能。
- 缓解措施和必需验证：保留方法调用失败的稳定错误码、固定 shortcut ID、后台 worker、GUI 非阻塞轮询和 tray/IPC 回退；在 KDE Wayland 与非 KDE Wayland 真实会话验证 v1/v2 backend、授权/拒绝、两项绑定、部分接受、重绑、Portal 重启、休眠恢复、退出清理、焦点、任务栏/Dock/分页器和帧时间。
- 回滚或隔离动作：若官方/实机证据确认 v1 完整兼容，独立任务中降低版本门槛并补方法契约；否则维持 v2 或对不完整 backend 隐藏 Portal 入口，不引入同步 D-Bus 或后台键盘监听。
- 负责人和状态：Neo；离线版本门槛与静态门禁已验证，真实 backend 方法/授权/桌面行为证据开放。

## R-063：用户级开机自启的真实登录与窗口驻留尚未验证（高）

- 证据和影响范围：104 将设置 schema 升级至 v9，开机自启默认关闭；设置保存先调用平台适配器，再以原子记录保存。Linux 使用用户级 XDG Autostart `.desktop`，Windows 使用当前用户 `HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run`，macOS 使用用户级 `~/Library/LaunchAgents/io.github.yhyzgn.pinora.plist` 兼容路径。启动项带 Pinora 所有权标记并显式传递 `--pinora-autostart`，已有实例不会触发截图；保存或热键重绑失败会尝试恢复旧启动项。离线测试覆盖 schema v1-v8 迁移、v9 往返、参数转义、`TryExec`、原子文件与所有权冲突；补偿调用路径已由源码审计，尚无独立故障注入测试。workspace 门禁和 Windows target 编译通过。
- 触发条件：用户目录权限/只读文件、桌面会话加载顺序、XDG 配置目录覆盖、Windows Run 注册表解析、macOS `launchd` 加载与用户批准、应用路径迁移/升级、单实例竞争，以及登录时托盘与辅助窗口映射的合成器时序。
- 失败模式：启动项可能写入成功但登录时未加载、重复启动仍触发错误动作、未知项被误处理，或辅助窗口短暂进入任务栏、Dock、分页器；CI、交叉编译和离线文件测试不能观察这些行为，也不能证明启动延迟或 Snipaste 级流畅度。当前 macOS 实现不是 macOS 13+ `SMAppService` 受管 LoginItem/Agent。
- 缓解措施和必需验证：维持用户显式开关、稳定所有权标记、绝对且存在的可执行文件、同目录临时文件 + `sync_all` + rename、平台失败不保存新值、未知项拒绝覆盖/删除和 `--pinora-autostart` tray-only 入口。在 Linux X11/KDE Wayland、Windows 和 macOS 原生登录会话验证启用/禁用/升级迁移/权限拒绝/已有实例竞争、启动时无截图、tray 常驻、任务栏/Dock/分页器、首帧、焦点和启动耗时；macOS 后续应评估迁移到 `SMAppService`。
- 回滚或隔离动作：隐藏设置入口并停止适配器调用，保留 v9 读取但不改变现有项；仅由应用关闭 Pinora 托管项，不删除未知启动项。若平台原生探针失败，保留 tray/IPC 手动入口并按稳定错误码反馈。
- 负责人和状态：Neo；离线契约与静态门禁已验证，真实登录、权限、窗口管理器、启动性能和 `SMAppService` 证据开放。
- 文档更正：Linux `.desktop` 的 `TryExec` 使用未引号的绝对可执行路径，`Exec` 单独负责参数转义；可执行文件在启用前必须是绝对且存在的普通文件，入口通过 `Exec` 传递 `--pinora-autostart`。

## R-064：桌面面板 crate 边界已收敛，但真实 GUI 仍未验证（中）

- 证据和影响范围：113 已将 `settings_panel`、`history_browser`、`diagnostics_panel`、`overlay_selection_readout` 和 `pin_context_menu` 迁入 `pinora-desktop`；`pinora-app` 通过 crate 内兼容 re-export 复用这些纯 UI 模块。`cargo tree -p pinora-desktop --depth 1` 仅显示 `pinora-core` 与 `winit`，`cargo tree -p pinora-app --depth 1` 仍显示对 `pinora-desktop` 的直接依赖；`cargo test -p pinora-desktop -- --nocapture` 77 项全部通过。
- 触发条件：真实窗口缩放、输入法、焦点、任务栏/Dock、HiDPI 或持续帧时间出现差异。
- 失败模式：离线布局、命中和 XRGB 绘制测试无法证明原生窗口管理器下的可用性或流畅度；真实 GUI 行为仍可能与纯数据测试不同。
- 缓解措施和必需验证：在授权的原生桌面会话复查设置、历史、诊断、读数和贴图菜单；保留 app 侧窗口宿主与业务服务不变。
- 回滚或隔离动作：恢复 app 内兼容 re-export 或回退纯 UI 模块引用，不触碰设置、历史、诊断或贴图数据格式。
- 负责人和状态：Neo；离线 crate 边界和定向测试已验证，真实 GUI、HiDPI、输入法、焦点、tray/taskbar 和性能证据开放。

## R-065：导出与剪贴板 crate 边界已收敛，但真实系统权限仍未验证（中）

- 证据和影响范围：114 已将 `image_sink` 与 `export_job` 迁入 `pinora-export`，导出 worker 继续通过 `pinora-jobs` 监督，app 仅保留请求、结果和关闭编排；`cargo test -p pinora-export -- --nocapture` 25 项通过，1 项真实显示会话剪贴板测试忽略。
- 触发条件：Windows/macOS/Linux 原生剪贴板权限、Wayland/X11 工具可用性、网络文件系统/权限错误、系统休眠或退出期间的子进程回收与原子 rename 时序。
- 失败模式：离线编码和 fake runner 测试通过，但真实剪贴板发布失败、目标文件权限不足、进程退出时 worker 未及时收敛或 GUI 反馈延迟。
- 缓解措施和必需验证：保持目标扩展名与质量校验、内存副本先行、系统剪贴板失败不报告成功、临时文件原子发布、取消/超时后 RAII 回收；在授权 Windows/macOS/X11/KDE Wayland 会话验证图像/文本复制、失败反馈、退出回收、HiDPI 连续导出和 tray-only 窗口隔离。
- 回滚或隔离动作：恢复 app 内 `image_sink`/`export_job` 模块和导入，不改变导出文件格式、历史索引和任务状态字符串。
- 负责人和状态：Neo；离线 crate 边界和 workspace 门禁已验证，真实系统剪贴板、权限、性能和原生窗口证据开放。

## R-066：历史工作流 crate 边界已收敛，但真实文件系统行为仍未验证（中）

- 证据和影响范围：115 已将 `history_export` 与 `history_load_job` 迁入 `pinora-history`；历史索引、受管 PNG 路径/摘要/尺寸校验、tombstone 清理和异步读取 worker 不再由 app 编译，app 仍持有历史窗口和选择编排。`cargo test -p pinora-history -- --nocapture` 26 项全部通过。
- 触发条件：Windows/macOS/Linux 原生文件权限、断电/崩溃、网络文件系统、符号链接、多个实例竞争或大图读取与 GUI 事件循环时序不同。
- 失败模式：离线测试通过但索引原子保存、tombstone 清理、摘要读取或单 worker 取消在真实文件系统上失败；历史窗口可能出现首帧/焦点/延迟问题。
- 缓解措施和必需验证：保持受管目录单层文件名白名单、先索引后清理、失败回滚、摘要/尺寸/颜色通道严格校验、owner/generation/终态门禁；在授权原生会话验证权限拒绝、断电恢复、损坏文件、并发启动、历史预览/重开/编辑和 tray-only 窗口隔离。
- 回滚或隔离动作：恢复 app 内历史模块与导入，移除 `pinora-history` workspace 成员，不改变索引 schema、文件格式和窗口策略。
- 负责人和状态：Neo；离线 crate 边界和 workspace 门禁已验证，真实文件系统、权限、GUI 和性能证据开放。

## R-067：托盘适配 crate 边界已收敛，但真实原生 tray 仍未验证（高）

- 证据和影响范围：116 已将 `tray.rs` 迁入 `pinora-tray`，该 crate 唯一声明 `tray-icon` 及 Linux GTK 依赖；app 仅消费 `TrayAction` 并保持唯一 EventLoop。`cargo test -p pinora-tray -- --nocapture` 15 项全部通过。
- 触发条件：Windows/macOS/X11/KDE Wayland 的 tray 协议、GTK 初始化、StatusNotifier/AppIndicator 可用性、菜单回调、桌面重连、任务栏/Dock 与窗口列表时序。
- 失败模式：代码与离线 MenuId 测试正确但托盘不可见、菜单失效、动态贴图列表未刷新、退出后残留图标，或辅助窗口仍进入任务栏/Dock。
- 缓解措施和必需验证：保持托盘构建失败为受控启动失败、单实例单 tray、动态列表无内容泄露、动作仅由 app EventLoop 编排；在授权原生会话验证启动/退出、所有菜单项、能力/反馈刷新、托盘重连、Overlay/贴图/设置/历史的任务栏/Dock/分页器隔离与焦点。
- 回滚或隔离动作：恢复 app 内 `tray.rs` 和依赖声明，移除 `pinora-tray` workspace 成员；不改变截图、贴图、历史、设置、窗口策略或数据格式。
- 负责人和状态：Neo；离线 crate 边界和 workspace 门禁已验证，真实原生 tray、窗口管理器和性能证据开放。

## 风险一：上下文漂移

- 触发条件：代码、配置或工作流变化后，没有同步更新对应上下文文件。
- 缓解措施：根据变化的事实或生命周期状态更新 `system/`、计划或任务。
- 回滚方式：只回滚上下文变更，不改变源码行为。

## 风险二：遗留迁移丢失

- 触发条件：未做语义分类就机械移动 `prompt.md`、`.memory/` 或 `.prompt/`。
- 缓解措施：先生成迁移清单；所有长期事实和可执行任务完成核对前保留来源文件。
- 回滚方式：从未改动的来源文件恢复，只删除生成的上下文文件。

## 风险三：外部依赖副作用

- 触发条件：校验或测试连接共享数据库、队列、缓存或远程接口。
- 缓解措施：优先离线检查，只运行明确授权的隔离验证。
- 回滚方式：产生副作用前停止，并在当前任务中记录阻塞证据。
