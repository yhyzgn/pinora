[package]
name = "pinora-platform"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Pinora system integration: startup, single instance, IPC and global hotkeys"

[dependencies]
pinora-core = { workspace = true }
fs2 = "0.4"
global-hotkey = "0.7"
winit = "0.30"

[target.'cfg(target_os = "linux")'.dependencies]
async-channel = "2"
futures-lite = "2"
zbus = "5.18.0"
